---
phase: 08-orchestrator-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/step-functions/orchestrator/refresh_orchestrator.asl.json
  - main.tf
  - tests/snapshots/refresh_orchestrator_outputs.json
autonomous: true
requirements: [ORCH-04, ORCH-05]

must_haves:
  truths:
    - "Un bloc input S3 optionnel (Enabled/Source/Destinations[]/Replication/Backfill) pilote une phase de replication S3 dans l'orchestrateur (mirroir du bloc EFS)"
    - "Quand le bloc S3 est absent ou S3.Enabled=false, la branche S3 atteint immediatement un Succeed no-op : le reachable-set des etats actifs est strictement identique a avant"
    - "Quand S3.Enabled=true, l'orchestrateur invoque setup_cross_account_replication via startExecution.sync:2 avec la config reshapee vers le contrat fige Phase 7"
    - "Quand S3.Backfill.Enabled=true, l'orchestrateur invoque run_batch_replication puis check_batch_replication via sync:2, en re-extrayant le JobId entre les deux"
    - "Le setup S3 + backfill chevauchent les phases de refresh longues (branche tissee dans Phase1DataRefresh Parallel)"
    - "scripts/validate_asl.py passe sur refresh_orchestrator.asl.json (JSON valide, toutes references Next/Default/Catch resolues, aucun etat orphelin)"
    - "Le snapshot d'interface refresh_orchestrator est regenere et tests/test_interface_snapshots.py passe"
  artifacts:
    - path: "modules/step-functions/orchestrator/refresh_orchestrator.asl.json"
      provides: "Branche S3 self-guarded (CheckS3Enabled) tissee dans le Parallel Phase1DataRefresh + reshape vers contrats figes"
      contains: "CheckS3Enabled"
    - path: "main.tf"
      provides: "Instanciation du module step_functions_s3 a la racine (Style A : ARNs via execution-input StepFunctions.S3.*)"
      contains: "step_functions_s3"
    - path: "tests/snapshots/refresh_orchestrator_outputs.json"
      provides: "Snapshot d'interface regenere apres ajout des etats terminaux S3"
  key_links:
    - from: "refresh_orchestrator.asl.json (branche S3)"
      to: "$.StepFunctions.S3.SetupCrossAccountReplication"
      via: "StateMachineArn.$ sur startExecution.sync:2"
      pattern: "StepFunctions\\.S3\\."
    - from: "run_batch (S3 sub-SFN output $.<RP>.Output.JobId)"
      to: "check_batch input $.BatchJob.JobId"
      via: "Pass/ResultSelector re-extraction entre les deux Tasks"
      pattern: "Output\\.JobId"
    - from: "main.tf module step_functions_s3"
      to: "module.step_functions_s3.step_function_arns"
      via: "output map cle setup_cross_account_replication/run_batch_replication/check_batch_replication"
      pattern: "module\\.step_functions_s3"
---

<objective>
Tisser la replication S3 cross-account (4 SFN livrees Phase 7, contrats FIGES) dans `refresh_orchestrator.asl.json` de maniere **optionnelle**, en miroir exact du pattern EFS deja present dans le meme fichier. La phase S3 est tissee comme une nouvelle branche self-guarded dans le Parallel `Phase1DataRefresh` (chevauchement des phases de refresh longues, D-01/D-02), avec une garde `CheckS3Enabled` garantissant un comportement strictement identique a avant quand le bloc S3 est absent ou desactive (ORCH-05/D-03). Le root `main.tf` instancie le module `step_functions_s3` pour que ses ARNs existent ; les ARNs transitent via l'execution-input `$.StepFunctions.S3.*` (Style A, aucun changement du module orchestrator Terraform).

Purpose: Permettre a l'orchestrateur de configurer + backfiller la replication S3 pendant un refresh, sans regression sur les consumers actuels (S3 nouveau, no-op par defaut).
Output: refresh_orchestrator.asl.json (branche S3 + reshape), main.tf (module step_functions_s3), snapshot d'interface regenere.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-orchestrator-integration/08-CONTEXT.md
@.planning/phases/08-orchestrator-integration/08-PATTERNS.md

<interfaces>
<!-- Contrats FIGES Phase 7 (READ-ONLY) — formes d'input exactes que l'orchestrateur doit produire par reshape. Ne JAMAIS modifier ces fichiers. -->

setup_cross_account_replication (StartAt: GetSourceVersioning) attend :
  - $.SourceBucket            (nom du bucket source, pas l'ARN)
  - $.Destinations[]          (chaque element : Bucket, AccountId, RoleArn, Region, StorageClass?, Prefix?, RTC?, Metrics?, DeleteMarkerReplication?)
  - $.ReplicationRoleArn      (role de replication live ; PassRole)
  - $.SourceAccount.RoleArn   (role assume imperativement DANS la sous-SFN via Credentials.RoleArn.$)
  Sortie : Succeed (setup) ; pas de champ structurant consomme par l'orchestrateur.

run_batch_replication (StartAt: CheckReplicationExists) attend :
  - $.SourceBucket            (nom)
  - $.SourceBucketArn         (ARN, pour S3JobManifestGenerator.SourceBucket)
  - $.BatchReplicationRoleArn (RoleArn du createJob)
  - $.ReportBucketArn?        (optionnel ; absent => Report.Enabled:false)
  - $.SourceAccount.AccountId (ExpectedBucketOwner + AccountId du job)
  - $.SourceAccount.RoleArn   (Credentials.RoleArn.$)
  Sortie (PrepareOutput -> $.Output) : { Status, ModuleName, JobId }  =>  apres sync:2 sous ResultPath $.X : $.X.Output.JobId
  IMPORTANT : le ManifestGenerator filtre deja ObjectReplicationStatuses ["NONE","FAILED"] + EligibleForReplication:true. NE PAS passer d'input qui override ce filtre (le contrat n'a aucun champ manifest-override — ne pas en inventer).

check_batch_replication (StartAt: WaitForJob, TimeoutSeconds:86400) attend :
  - $.BatchJob.JobId          (l'orchestrateur DOIT placer le JobId ici)
  - $.SourceAccount.AccountId (AccountId du describeJob)
  - $.SourceAccount.RoleArn   (Credentials.RoleArn.$)

Sortie du module s3/ (outputs.tf) : output "step_function_arns" = map { setup_cross_account_replication, run_batch_replication, check_batch_replication, delete_replication } => ARN
</interfaces>

<asl_analog>
<!-- Analog EFS DANS LE MEME FICHIER (refresh_orchestrator.asl.json). Mirror exact. -->
- Host Parallel : "Phase1DataRefresh" (lignes ~649-1129) — Parallel a 3 branches. Branche C ("CheckEFSEnabled" -> chaine EFS) est self-guarded et Succeed quand desactivee. C'est le precedent direct pour la nouvelle branche S3.
- Garde no-op in-branch : "CheckEFSEnabled" (lignes ~883-901) — Choice : si $.EFS.Enabled IsPresent ET BooleanEquals:false -> "EFSRefreshComplete" (Succeed) ; Default -> travail. (Le bloc absent echoue IsPresent et tombe sur Default ; pour S3 on inverse : Default = no-op Succeed, voir action Task 1.)
- Appel sync:2 (style JSONPath) : "SetupCrossAccountReplication" (lignes ~924-958) — Resource arn:aws:states:::states:startExecution.sync:2, StateMachineArn.$ depuis $.StepFunctions.EFS.*, Input map, Name.$ via States.Format('{}-...', $$.Execution.Name), ResultPath capture, Catch -> Fail.
- Re-extraction enveloppe .sync:2 : EFS lit $.ReplicationSetupResult.Output.DestinationFileSystemId (ligne ~967) ; ResultSelector idiom a ValidateInputs (lignes ~57-61).
- Sous-toggle Choice : "CheckStoreInSSMOption" (lignes ~1059-1077) — Choice sur sous-flag IsPresent+BooleanEquals:true -> Task ; Default -> Succeed. Modele pour S3.Backfill.Enabled.
- Root main.tf : "module step_functions_efs" (lignes ~42-52) + bloc "module orchestrator" (lignes ~117-134) cablant les outputs des sous-modules.
</asl_analog>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tisser la branche S3 self-guarded dans Phase1DataRefresh (garde CheckS3Enabled + chaine setup/backfill + reshape)</name>
  <read_first>
    - modules/step-functions/orchestrator/refresh_orchestrator.asl.json (TOUT le Parallel Phase1DataRefresh lignes ~649-1129 ; mirror exact des branches existantes : structure Branches[], CheckEFSEnabled, SetupCrossAccountReplication, ResultPath, Catch -> Fail)
    - modules/step-functions/s3/setup_cross_account_replication.asl.json (forme d'input figee — section interfaces ci-dessus)
    - modules/step-functions/s3/run_batch_replication.asl.json (forme d'input + sortie $.Output.JobId + filtre manifest NONE/FAILED a NE PAS regresser)
    - modules/step-functions/s3/check_batch_replication.asl.json (input $.BatchJob.JobId + $.SourceAccount.*)
    - .planning/phases/08-orchestrator-integration/08-PATTERNS.md (sections 1a a 1h)
  </read_first>
  <action>
    Ajouter une **4eme branche** au tableau `Branches` du Parallel `Phase1DataRefresh` (ne PAS toucher aux 3 branches existantes A/B/C, ne PAS changer son `ResultPath: $.Phase1Results`, son `Next: "CheckRunSqlScriptsOption"`, ni son `Catch` top-level). La nouvelle branche commence a un etat `CheckS3Enabled`.

    Garde no-op (D-03/ORCH-05) — `CheckS3Enabled` est un `Choice` : une seule `Choices` entry avec un `And` de deux conditions ($.S3.Enabled `IsPresent: true` ET $.S3.Enabled `BooleanEquals: true`) -> `Next: "S3SetupReplication"`. `Default: "S3ReplicationComplete"` (un `Succeed`). Ainsi un run SANS bloc S3 (cle absente => IsPresent false) OU avec `S3.Enabled=false` atteint immediatement le Succeed = aucun appel S3, aucun effet de bord. NE PAS garder sur le cross-account (la replication S3 est toujours cross-account par design) — garder uniquement sur $.S3.Enabled.

    `S3SetupReplication` (Task, startExecution.sync:2, mirror de SetupCrossAccountReplication) : `Resource: "arn:aws:states:::states:startExecution.sync:2"`, `StateMachineArn.$: "$.StepFunctions.S3.SetupCrossAccountReplication"` (Style A, voir Task 2). Reshape `Input` (D-10) vers le contrat fige : `SourceBucket.$: "$.S3.Source.Bucket"`, `Destinations.$: "$.S3.Destinations"`, `ReplicationRoleArn.$: "$.S3.Replication.RoleArn"`, et `SourceAccount.$: "$.S3.Source.Account"` (l'objet portant `.AccountId` et `.RoleArn`). `Name.$: "States.Format('{}-S3Setup', $$.Execution.Name)"`. `ResultPath: "$.S3SetupResult"`. `Next: "CheckS3BackfillEnabled"`. `Catch [States.ALL] -> ResultPath "$.S3Error" -> "S3ReplicationFailed"`. NE PAS poser de bloc `Credentials` sur cette Task (l'assume-role se fait DANS la sous-SFN). `setup` est TOUJOURS invoque quand S3.Enabled (D-04).

    `CheckS3BackfillEnabled` (Choice, sous-toggle D-05, mirror CheckStoreInSSMOption) : `Choices` entry `And`($.S3.Backfill.Enabled IsPresent:true, $.S3.Backfill.Enabled BooleanEquals:true) -> `Next: "S3RunBatch"` ; `Default: "S3ReplicationComplete"` (skip backfill, Succeed).

    `S3RunBatch` (Task, sync:2) : `StateMachineArn.$: "$.StepFunctions.S3.RunBatchReplication"`. Reshape `Input` : `SourceBucket.$: "$.S3.Source.Bucket"`, `SourceBucketArn.$: "$.S3.Source.BucketArn"`, `BatchReplicationRoleArn.$: "$.S3.Replication.BatchReplicationRoleArn"`, `SourceAccount.$: "$.S3.Source.Account"`. (Ne PAS ajouter de champ de manifest — le filtre NONE/FAILED reste cote contrat, D-08/specifics.) `ReportBucketArn` : optionnel — l'omettre du reshape (le contrat traite l'absence via ShouldEnableReport), sauf si tu choisis de le passer conditionnellement via JSONata `$merge` ; la voie la plus simple est de l'omettre. `Name.$: States.Format('{}-S3RunBatch', $$.Execution.Name)`. `ResultPath: "$.S3BatchResult"`. `Next: "S3PrepareJobId"`. `Catch -> "$.S3Error" -> "S3ReplicationFailed"`.

    `S3PrepareJobId` (Pass, re-extraction de l'enveloppe .sync:2, D-10/section 1d) : la sortie de run_batch est sous `$.S3BatchResult.Output.JobId`. check_batch attend `$.BatchJob.JobId`. Utiliser `Parameters` : `{ "BatchJob": { "JobId.$": "$.S3BatchResult.Output.JobId" } }` avec `ResultPath: "$.S3BatchJob"` — OU re-mapper directement dans l'Input du Task suivant. Choisir une forme et la documenter ; recommande : Pass produisant `$.S3BatchJob.JobId`. `Next: "S3CheckBatch"`.

    `S3CheckBatch` (Task, sync:2) : `StateMachineArn.$: "$.StepFunctions.S3.CheckBatchReplication"`. `Input` : `BatchJob` reshape vers `{ "JobId.$": "<chemin du JobId re-extrait>" }` et `SourceAccount.$: "$.S3.Source.Account"`. `Name.$: States.Format('{}-S3CheckBatch', $$.Execution.Name)`. `ResultPath: "$.S3CheckResult"`. `Next: "S3ReplicationComplete"`. `Catch -> "$.S3Error" -> "S3ReplicationFailed"`.

    `S3ReplicationComplete` (Succeed). `S3ReplicationFailed` (Fail, `Error: "S3ReplicationFailed"`, `Cause` descriptif).

    Aucun appel `delete_replication` (D-06, NO teardown). Aucune modification des SFN S3 figees. Conserver la convention de nommage EFS (PascalCase, prefixe S3, Fail states nommes).
  </action>
  <verify>
    <automated>python3 scripts/validate_asl.py modules/step-functions/orchestrator/refresh_orchestrator.asl.json</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/validate_asl.py` sort 0 sur refresh_orchestrator.asl.json (JSON valide ; toutes references Next/Default/Catch resolues ; aucun etat orphelin/unreachable).
    - L'etat `CheckS3Enabled` (Choice) existe ; sa garde combine $.S3.Enabled IsPresent:true ET BooleanEquals:true ; son `Default` pointe vers le Succeed no-op `S3ReplicationComplete`.
    - Les etats `S3SetupReplication`, `CheckS3BackfillEnabled`, `S3RunBatch`, `S3PrepareJobId`, `S3CheckBatch`, `S3ReplicationComplete` (Succeed), `S3ReplicationFailed` (Fail) existent, tous DANS une nouvelle branche du Parallel `Phase1DataRefresh`.
    - Les 3 Tasks S3 utilisent `arn:aws:states:::states:startExecution.sync:2` et `StateMachineArn.$` pointant vers `$.StepFunctions.S3.*` ; aucune Task S3 ne pose de bloc `Credentials`.
    - La re-extraction du JobId est presente : un chemin `*.Output.JobId` issu de run_batch alimente le `BatchJob.JobId` de check_batch (grep `Output\.JobId` matche ; le `Input` de S3CheckBatch contient `BatchJob`).
    - ORCH-05 (no-op) : avec un input SANS cle `S3`, la seule transition active de la branche est `CheckS3Enabled` -> `S3ReplicationComplete` (Succeed) ; aucun etat `S3SetupReplication`/`S3RunBatch`/`S3CheckBatch` n'est atteignable sur ce chemin. Les 3 branches EFS/DB/Dump existantes sont inchangees (diff limite a l'ajout d'une branche dans `Branches[]`).
    - Le filtre manifest n'est pas regresse : aucun champ de manifest/override n'est passe dans l'Input de `S3RunBatch` (grep `ManifestGenerator|ObjectReplicationStatuses` => 0 occurrence cote orchestrateur).
    - Aucune reference a `delete_replication` / `DeleteReplication` dans la branche S3.
  </acceptance_criteria>
  <done>
    La branche S3 est tissee dans Phase1DataRefresh avec garde no-op stricte, chaine setup -> (backfill conditionnel run_batch -> JobId reshape -> check_batch), reshape vers les contrats figes, et validate_asl.py passe.
  </done>
</task>

<task type="auto">
  <name>Task 2: Instancier le module step_functions_s3 a la racine (Style A) et regenerer le snapshot d'interface</name>
  <read_first>
    - main.tf (blocs `module "step_functions_efs"` lignes ~42-52 et `module "orchestrator"` lignes ~117-134 — mirror exact des inputs prefix/tags/orchestrator_role_arn/enable_logging/log_retention_days/enable_xray_tracing)
    - modules/step-functions/s3/variables.tf (confirmer que les inputs attendus matchent le bloc EFS : prefix, tags, orchestrator_role_arn, enable_logging, log_retention_days, enable_xray_tracing, naming_convention)
    - modules/step-functions/s3/outputs.tf (output step_function_arns map)
    - tests/test_interface_snapshots.py (mecanique : extract_output_schema capture etats terminaux Succeed/Pass-avant-Succeed + Comment top-level ; bootstrap si snapshot absent, sinon assert egalite stricte)
    - tests/snapshots/refresh_orchestrator_outputs.json (snapshot actuel a regenerer)
  </read_first>
  <action>
    Dans le root `main.tf`, ajouter un bloc `module "step_functions_s3"` en miroir de `module "step_functions_efs"` : `source = "./modules/step-functions/s3"`, et les inputs `prefix`, `tags`, `orchestrator_role_arn = module.iam.orchestrator_role_arn`, `enable_logging`, `log_retention_days`, `enable_xray_tracing` (memes valeurs var.* que les autres sous-modules). Cela rend les ARNs des 4 SFN S3 disponibles via `module.step_functions_s3.step_function_arns`.

    Style A (D-discretion, recommande par PATTERNS.md Target 2) : NE PAS ajouter de variable `s3_step_function_arns` au module orchestrator ni de cle au bloc `module "orchestrator"`. Les ARNs S3 transitent par l'execution-input `$.StepFunctions.S3.*` (exactement comme EFS/DB/EKS aujourd'hui, qui passent par `$.StepFunctions.*` et non par templatefile). Le cablage du bloc input reel (qui peuple StepFunctions.S3 a partir de module.step_functions_s3.step_function_arns) est cote caller/stack = HORS SCOPE de cette phase (cf. domain CONTEXT.md) ; le root doit seulement instancier le module pour que les ARNs existent et que `terraform validate` passe.

    Regenerer le snapshot d'interface : l'ajout des etats terminaux S3 (`S3ReplicationComplete` Succeed, et le Pass `S3PrepareJobId` si capture) et tout changement du Comment top-level de refresh_orchestrator vont faire diverger `tests/snapshots/refresh_orchestrator_outputs.json`. Supprimer ce fichier snapshot puis relancer `tests/test_interface_snapshots.py` en mode bootstrap pour le recreer a partir de l'ASL modifie (la suite recree le snapshot quand il est absent). Verifier ensuite que la suite passe au second run (assert egalite stricte). Documenter dans le SUMMARY que cette regeneration est attendue et NON une regression : c'est l'ajout volontaire d'etats terminaux S3, pas une mutation des sorties EFS/DB/EKS existantes.
  </action>
  <verify>
    <automated>terraform -chdir=. validate && rm -f tests/snapshots/refresh_orchestrator_outputs.json && python3 -m pytest tests/test_interface_snapshots.py tests/test_asl_validation.py -q && python3 -m pytest tests/test_interface_snapshots.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `terraform validate` (ou `tofu validate`) sort 0 a la racine ; le bloc `module "step_functions_s3"` existe avec `source = "./modules/step-functions/s3"` et les 6 inputs en miroir d'EFS.
    - Aucune variable `s3_step_function_arns` ajoutee au module orchestrator (Style A : grep `s3_step_function_arns` dans modules/step-functions/orchestrator/ => 0 ; grep dans le bloc `module "orchestrator"` du root main.tf => 0).
    - `tests/test_asl_validation.py` passe (auto-decouverte rglob couvre refresh_orchestrator.asl.json modifie + les 3 ASL S3).
    - Apres regeneration, `tests/test_interface_snapshots.py` passe au second run (egalite stricte) ; le snapshot regenere contient l'etat terminal `S3ReplicationComplete`.
    - La regeneration n'a PAS supprime/modifie les sorties terminales EFS/DB/EKS pre-existantes dans le snapshot (les cles terminal_outputs existantes restent presentes ; seules des cles S3 sont ajoutees).
  </acceptance_criteria>
  <done>
    Le module step_functions_s3 est instancie a la racine, `terraform validate` passe, et le snapshot d'interface est regenere avec la suite de tests verte (ASL validation + snapshots).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> orchestrator execution input | Le bloc `$.S3` (Source.Account.RoleArn, Replication.RoleArn, BatchReplicationRoleArn, Destinations[].RoleArn, AccountId, Bucket) est fourni par l'appelant dans l'input d'execution ; l'orchestrateur le reshape et le passe aux sous-SFN S3. |
| orchestrator -> sous-SFN S3 (startExecution.sync:2) | L'orchestrateur ne pose AUCUN `Credentials` ; l'assume-role cross-account se fait DANS chaque SFN S3 via `Credentials.RoleArn.$: $.SourceAccount.RoleArn` (contrat fige Phase 7). |
| garde d'activation CheckS3Enabled | Frontiere de controle : separe le chemin no-op (S3 absent/desactive) du chemin operationnel (appels SDK s3/s3control cross-account). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Elevation of Privilege | reshape orchestrateur -> SourceAccount.RoleArn / ReplicationRoleArn / BatchReplicationRoleArn | mitigate | L'orchestrateur ne fait que **transmettre** les ARNs fournis par l'appelant ; il ne les **assume pas** (aucun `Credentials` sur les Tasks S3). L'assume-role se fait dans la SFN S3 figee (pattern imperatif Credentials.RoleArn.$ deja securise + auditee Phase 7). Le perimetre d'escalade est borne par la trust policy des roles cibles (cote source-account/destination, hors orchestrateur). Aucun role privilegie n'est cable en dur dans l'ASL. |
| T-08-02 | Tampering / Bypass | garde CheckS3Enabled (ORCH-05) | mitigate | La garde exige `IsPresent:true` ET `BooleanEquals:true` ; un input sans cle S3 echoue IsPresent et tombe sur le Default no-op (Succeed). Impossible de declencher les ops S3 sans poser explicitement `S3.Enabled=true`. Verifie par validate_asl.py (reachabilite) + acceptance criterion no-op. La garde S3 est interne a la branche : meme si un attaquant pose un bloc S3 partiel, setup echoue proprement (Catch -> S3ReplicationFailed) sans toucher les autres branches. |
| T-08-03 | Tampering | filtre manifest NONE/FAILED de run_batch | mitigate | L'orchestrateur ne passe aucun champ de manifest/override (le contrat fige n'en expose pas) ; le filtre `ObjectReplicationStatuses:["NONE","FAILED"]` + `EligibleForReplication:true` reste cote SFN. Acceptance criterion : grep 0 occurrence cote orchestrateur. Empeche un re-run de recopier/double-egress l'integralite du bucket. |
| T-08-04 | Information Disclosure | Destinations[] (AccountId/Bucket cross-account) dans l'input | accept | Les identifiants de destination sont des references de ressources (pas des secrets) ; ils sont deja requis par le contrat EFS-mirror et par les SFN S3 figees. Pas de PII, pas de credential en clair (seuls des ARNs de roles, dont l'usage est borne par leur trust policy). |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | Aucun package installe dans cette phase (modification ASL/Terraform + regeneration de snapshot uniquement ; pytest/terraform deja presents). Pas de gate de legitimite applicable. |

**Severite haute :** Aucune menace de severite haute identifiee. Le seul vecteur d'escalade (T-08-01) est mitige structurellement : l'orchestrateur transmet des ARNs sans les assumer, l'assume-role reste confine aux SFN figees Phase 7, et la garde CheckS3Enabled (T-08-02) empeche tout declenchement non explicite. Aucun blocage requis.
</threat_model>

<verification>
- `python3 scripts/validate_asl.py modules/step-functions/orchestrator/refresh_orchestrator.asl.json` => exit 0 (JSON valide, refs resolues, aucun orphelin).
- `terraform validate` (ou `tofu validate`) a la racine => exit 0.
- `python3 -m pytest tests/test_asl_validation.py tests/test_interface_snapshots.py -q` => vert (apres regeneration du snapshot).
- ORCH-05 no-op : un input sans bloc S3 ne rend atteignable que `CheckS3Enabled -> S3ReplicationComplete` dans la branche S3 ; les branches EFS/DB/Dump et le reste du graphe sont structurellement inchanges.
</verification>

<success_criteria>
- ORCH-04 : un bloc input S3 optionnel (Enabled/Source/Destinations[]/Replication/Backfill, mirroir EFS) pilote la phase de replication S3 dans l'orchestrateur.
- ORCH-05 : bloc S3 absent ou S3.Enabled=false => branche reduite a un Succeed no-op, comportement strictement identique a avant (reachable-set inchange, prouve par validate_asl.py + criterion no-op).
- Critere #3 : S3.Enabled=true => setup invoque via sync:2 ; S3.Backfill.Enabled=true => run_batch puis check_batch via sync:2 avec JobId re-extrait ; aucun teardown.
- validate_asl.py, test_asl_validation.py et test_interface_snapshots.py passent.
</success_criteria>

<output>
Create `.planning/phases/08-orchestrator-integration/08-01-SUMMARY.md` when done
</output>
