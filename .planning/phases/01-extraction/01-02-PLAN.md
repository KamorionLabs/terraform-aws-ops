---
phase: 01-extraction
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - modules/step-functions/efs/manage_filesystem_policy.asl.json
  - modules/step-functions/efs/main.tf
  - modules/step-functions/efs/README.md
autonomous: true
requirements: [SUB-03, SUB-04, SUB-05, SUB-06, PRE-02]

must_haves:
  truths:
    - "ManageFileSystemPolicy sous-SFN existe comme fichier ASL valide avec ~6 states"
    - "La sous-SFN gere ses erreurs via un state Fail nomme ManageFileSystemPolicyFailed"
    - "Le contrat Input/Output est documente dans le champ Comment ASL et dans README.md"
    - "Les 3 cles sous-SFN sont enregistrees dans local.step_functions de main.tf"
    - "Le pattern templatefile() est documente dans README pour les appelants Phase 2"
    - "pytest test_asl_validation.py passe avec le nouveau fichier auto-decouvert"
  artifacts:
    - path: "modules/step-functions/efs/manage_filesystem_policy.asl.json"
      provides: "Sous-SFN ManageFileSystemPolicy extraite de setup_cross_account_replication et delete_replication"
      contains: "ManageFileSystemPolicyFailed"
    - path: "modules/step-functions/efs/main.tf"
      provides: "3 nouvelles cles dans local.step_functions (manage_filesystem_policy, manage_access_point, manage_lambda_lifecycle)"
      contains: "manage_filesystem_policy"
    - path: "modules/step-functions/efs/README.md"
      provides: "Documentation I/O contracts + pattern templatefile() pour Phase 2"
      contains: "ManageFileSystemPolicy"
  key_links:
    - from: "modules/step-functions/efs/main.tf"
      to: "modules/step-functions/efs/manage_filesystem_policy.asl.json"
      via: "local.step_functions map entry + file() function"
      pattern: 'manage_filesystem_policy.*=.*"manage_filesystem_policy.asl.json"'
    - from: "modules/step-functions/efs/outputs.tf"
      to: "modules/step-functions/efs/main.tf"
      via: "for_each automatique — ARN exporte sans changement"
      pattern: "step_function_arns"
---

<objective>
Extraire la premiere sous-SFN (ManageFileSystemPolicy), enregistrer les 3 cles Terraform, et creer la documentation.

Purpose: ManageFileSystemPolicy est la sous-SFN la plus propre (0 reference $$.Execution.Input dans ses sources). L'extraire en premier valide le pattern complet (ASL + Terraform + tests) avant de passer aux deux autres. Ce plan enregistre aussi les 3 cles dans main.tf d'un coup pour eviter des modifications incrementales.

Output: manage_filesystem_policy.asl.json fonctionnel, main.tf avec les 3 nouvelles cles, README.md avec contrats I/O et pattern templatefile.
</objective>

<execution_context>
@/Users/clark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/clark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-extraction/01-CONTEXT.md
@.planning/phases/01-extraction/01-RESEARCH.md
@.planning/phases/01-extraction/01-01-SUMMARY.md

<interfaces>
<!-- Existing code contracts from the EFS module -->

From modules/step-functions/efs/main.tf:
```hcl
locals {
  step_functions = {
    delete_filesystem = "delete_filesystem.asl.json"
    create_filesystem = "create_filesystem.asl.json"
    get_subpath_and_store_in_ssm = "get_subpath_and_store_in_ssm.asl.json"
    restore_from_backup              = "restore_from_backup.asl.json"
    check_replication_sync           = "check_replication_sync.asl.json"
    setup_cross_account_replication  = "setup_cross_account_replication.asl.json"
    delete_replication               = "delete_replication.asl.json"
  }
  # Naming: converts snake_case keys to kebab-case or PascalCase
  sfn_names = {
    for k, v in local.step_functions : k => (
      var.naming_convention == "pascal"
      ? "${var.prefix}-EFS-${replace(title(replace(k, "_", " ")), " ", "")}"
      : "${var.prefix}-efs-${replace(k, "_", "-")}"
    )
  }
}

resource "aws_sfn_state_machine" "efs" {
  for_each   = local.step_functions
  name       = local.sfn_names[each.key]
  role_arn   = var.orchestrator_role_arn
  definition = file("${path.module}/${each.value}")
  # ... logging, tracing, tags
}
```

From modules/step-functions/efs/outputs.tf:
```hcl
output "step_function_arns" {
  value = { for k, v in aws_sfn_state_machine.efs : k => v.arn }
}
output "step_function_names" {
  value = { for k, v in aws_sfn_state_machine.efs : k => v.name }
}
```

<!-- Source blocks to extract from (FileSystemPolicy pattern) -->

From modules/step-functions/efs/setup_cross_account_replication.asl.json (pattern a extraire):
- States "UpdateSourcePolicy" et "UpdateDestinationPolicy" gerent les filesystem policies
- Chaque state utilise aws-sdk:efs:putFileSystemPolicy avec Credentials cross-account
- Pattern: describeFileSystemPolicy -> putFileSystemPolicy (ajout/retrait de statements)
- 2 occurrences dans setup_cross_account_replication (source + destination)

From modules/step-functions/efs/delete_replication.asl.json (pattern a extraire):
- State "CleanupSourcePolicy" gere le retrait de la policy source apres suppression replication
- Meme pattern: describeFileSystemPolicy -> putFileSystemPolicy
- 1 occurrence dans delete_replication

<!-- Pattern ASL sous-SFN (from 01-RESEARCH.md) -->
Structure cible:
```json
{
  "Comment": "ManageFileSystemPolicy | Input: {...} | Output: {...}",
  "StartAt": "...",
  "States": {
    "...": { "Catch": [{"ErrorEquals": ["States.ALL"], "Next": "HandleError"}] },
    "HandleError": { "Type": "Fail", "Error": "ManageFileSystemPolicyFailed" }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Creer manage_filesystem_policy.asl.json</name>
  <files>modules/step-functions/efs/manage_filesystem_policy.asl.json</files>
  <action>
Creer le fichier `modules/step-functions/efs/manage_filesystem_policy.asl.json` en extrayant le pattern de gestion de FileSystem Policy depuis `setup_cross_account_replication.asl.json` et `delete_replication.asl.json`.

**Analyse des sources avant ecriture :**
1. Lire `setup_cross_account_replication.asl.json` en entier — identifier les states qui gerent les filesystem policies (pattern describeFileSystemPolicy / putFileSystemPolicy / deleteFileSystemPolicy)
2. Lire `delete_replication.asl.json` en entier — identifier le state de cleanup policy
3. Identifier le contrat Input commun : quelles donnees ces states utilisent (FileSystemId, Account.RoleArn, PolicyAction, Statement, etc.)
4. Verifier qu'AUCUN de ces states ne reference `$$.Execution.Input` (deja confirme propre dans l'audit)

**Structure de la sous-SFN (~6 states) :**
- Champ `Comment` obligatoire avec format: `"ManageFileSystemPolicy | Input: {FileSystemId, Account, Action, PolicyStatement} | Output: {FileSystemId, PolicyApplied, Status}"`
- `StartAt` pointe vers le premier state
- States logiques attendus (a adapter selon le code source exact) :
  1. Initialisation/parametrage de l'action (add/remove statement)
  2. DescribeFileSystemPolicy — lire la policy actuelle
  3. Logique de merge/remove du statement dans la policy
  4. PutFileSystemPolicy — appliquer la policy modifiee
  5. State de succes (End: true)
  6. HandleError — state `Fail` avec `Error: "ManageFileSystemPolicyFailed"`, `Cause: "See $.Error for details"`

**Regles SUB-05 (Catch auto-contenu) :**
- Chaque state Task a un bloc `Catch` avec `ErrorEquals: ["States.ALL"]`, `ResultPath: "$.Error"`, `Next: "HandleError"`
- Le state HandleError est de type Fail avec un Error nomme `ManageFileSystemPolicyFailed`

**Regles de contexte :**
- Utiliser `$.` pour toutes les references (jamais `$$.Execution.Input`)
- Les champs `Credentials` sont permis (cross-account via RoleArn)
- Le fichier utilise le QueryLanguage JSONPath (pas JSONata) sauf si les sources utilisent JSONata — dans ce cas suivre le meme langage

**Important :** Bien lire le code source complet des deux fichiers avant d'ecrire. Ne pas inventer de states — extraire le pattern reel depuis le code existant et le generaliser.
  </action>
  <verify>
    <automated>cd /Users/clark/work/kamorion/terraform-aws-ops && python -c "
import json
with open('modules/step-functions/efs/manage_filesystem_policy.asl.json') as f:
    d = json.load(f)
assert 'Comment' in d and 'ManageFileSystemPolicy' in d['Comment'], 'Missing Comment'
assert 'StartAt' in d, 'Missing StartAt'
assert d['StartAt'] in d['States'], 'StartAt refs missing state'
fail_states = [k for k,v in d['States'].items() if v.get('Type') == 'Fail']
assert len(fail_states) >= 1, 'No Fail state'
assert any('ManageFileSystemPolicyFailed' in d['States'][s].get('Error','') for s in fail_states), 'Fail state not named'
print(f'OK: {len(d[\"States\"])} states, Fail state present')
"</automated>
  </verify>
  <done>
    - manage_filesystem_policy.asl.json existe avec ~6 states
    - Champ Comment present avec contrat I/O
    - State Fail nomme ManageFileSystemPolicyFailed
    - Aucune reference $$.Execution.Input
    - JSON syntaxiquement valide
  </done>
</task>

<task type="auto">
  <name>Task 2: Enregistrer les 3 sous-SFN dans main.tf et creer README.md</name>
  <files>modules/step-functions/efs/main.tf, modules/step-functions/efs/README.md</files>
  <action>
1. Dans `modules/step-functions/efs/main.tf`, ajouter les 3 cles dans `local.step_functions` (section apres la ligne `delete_replication`) :

   ```hcl
   # Sub-SFNs — Phase 1 Extraction (reusable building blocks)
   manage_lambda_lifecycle  = "manage_lambda_lifecycle.asl.json"
   manage_access_point      = "manage_access_point.asl.json"
   manage_filesystem_policy = "manage_filesystem_policy.asl.json"
   ```

   ATTENTION : ajouter les 3 cles d'un coup meme si seul manage_filesystem_policy a son fichier ASL. Terraform plan echouera tant que les 2 autres fichiers n'existent pas, mais c'est intentionnel — ils seront crees dans Plan 03. Pour eviter ce blocage temporaire, creer des fichiers stub minimaux pour manage_lambda_lifecycle.asl.json et manage_access_point.asl.json :

   ```json
   {
     "Comment": "STUB - ManageLambdaLifecycle | To be implemented in Plan 03",
     "StartAt": "Placeholder",
     "States": {
       "Placeholder": {
         "Type": "Fail",
         "Error": "NotImplemented",
         "Cause": "This sub-SFN will be implemented in Plan 03"
       }
     }
   }
   ```

   (Meme pattern pour manage_access_point.asl.json avec le nom adapte.)

   Ne pas modifier le reste de main.tf (resource, naming, logging, tags — tout est gere automatiquement par le for_each).

2. Creer `modules/step-functions/efs/README.md` avec la documentation des contrats I/O pour les 3 sous-SFN :

   Structure du README :
   - Titre: `# EFS Sub-State Machines`
   - Introduction: Ce module contient les sous-SFN reutilisables pour les operations EFS. Elles sont deployees via le for_each existant dans main.tf.
   - Section par sous-SFN: ManageFileSystemPolicy (complete), ManageLambdaLifecycle (a venir), ManageAccessPoint (a venir)
   - Pour ManageFileSystemPolicy : documenter le schema Input (FileSystemId, Account avec RoleArn, Action, PolicyStatement), le schema Output (FileSystemId, PolicyApplied, Status), et le comportement Catch (Error: ManageFileSystemPolicyFailed)
   - Pour les 2 autres : mettre un placeholder `> To be documented after implementation in Plan 03`
   - Section "Integration Pattern" expliquant comment les appelants (Phase 2) utiliseront `templatefile()` pour injecter les ARNs des sous-SFN :
     ```hcl
     # Phase 2 pattern — appelants injectent les ARNs via templatefile()
     definition = templatefile("${path.module}/caller.asl.json", {
       manage_filesystem_policy_arn = module.efs_sfn.step_function_arns["manage_filesystem_policy"]
     })
     ```
   - Section "Naming" : les cles snake_case dans local.step_functions sont converties automatiquement. Exemple: `manage_filesystem_policy` → `{prefix}-efs-manage-filesystem-policy` (kebab) ou `{prefix}-EFS-ManageFilesystemPolicy` (pascal).
  </action>
  <verify>
    <automated>cd /Users/clark/work/kamorion/terraform-aws-ops && python -c "
import json
# Check main.tf has all 3 keys
with open('modules/step-functions/efs/main.tf') as f:
    content = f.read()
for key in ['manage_lambda_lifecycle', 'manage_access_point', 'manage_filesystem_policy']:
    assert key in content, f'Missing key: {key}'
# Check all 3 ASL files exist and are valid JSON
for name in ['manage_filesystem_policy', 'manage_access_point', 'manage_lambda_lifecycle']:
    with open(f'modules/step-functions/efs/{name}.asl.json') as f:
        d = json.load(f)
    assert 'StartAt' in d, f'{name}: missing StartAt'
# Check README exists
import os
assert os.path.exists('modules/step-functions/efs/README.md'), 'README.md missing'
print('OK: main.tf has 3 keys, all ASL files valid, README exists')
" && pytest tests/test_asl_validation.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>
    - main.tf contient les 3 nouvelles cles dans local.step_functions
    - manage_filesystem_policy.asl.json est complet, les 2 autres sont des stubs valides
    - README.md documente le contrat I/O de ManageFileSystemPolicy et le pattern templatefile()
    - pytest test_asl_validation.py passe (les 3 nouveaux fichiers sont auto-decouverts et valides)
    - outputs.tf n'a pas ete modifie (les ARNs sont exportes automatiquement via for_each)
  </done>
</task>

</tasks>

<verification>
1. `pytest tests/test_asl_validation.py -v --tb=short` — tous les tests passent, y compris les 3 nouveaux fichiers auto-decouverts
2. `python -c "import json; [json.load(open(f'modules/step-functions/efs/{n}.asl.json')) for n in ['manage_filesystem_policy', 'manage_access_point', 'manage_lambda_lifecycle']]"` — les 3 fichiers sont du JSON valide
3. `grep -c 'manage_' modules/step-functions/efs/main.tf` — au moins 3 occurrences
4. `grep '$$.Execution.Input' modules/step-functions/efs/manage_filesystem_policy.asl.json` — 0 occurrences (pas de scope loss)
</verification>

<success_criteria>
- ManageFileSystemPolicy est une sous-SFN complete avec Catch auto-contenu et Comment documente
- Les 3 cles sont dans main.tf (manage_filesystem_policy complete, 2 stubs)
- README.md documente les contrats I/O et le pattern templatefile pour Phase 2
- Tous les tests passent
- Aucune modification a outputs.tf ou variables.tf (for_each gere tout)
</success_criteria>

<output>
After completion, create `.planning/phases/01-extraction/01-02-SUMMARY.md`
</output>
