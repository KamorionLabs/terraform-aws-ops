---
phase: 04-foundation
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - modules/step-functions/sync/main.tf
  - modules/step-functions/sync/variables.tf
  - modules/step-functions/sync/outputs.tf
  - modules/step-functions/sync/versions.tf
  - main.tf
  - outputs.tf
autonomous: true
requirements: [INFRA-01]

must_haves:
  truths:
    - "Le module Terraform modules/step-functions/sync/ deploie la SFN SyncConfigItems et la Lambda sync_config_items avec tous les resources IAM, CloudWatch, et permissions"
    - "Le module est wire dans root main.tf comme les autres modules step-functions (db, efs, eks, utils) avec les memes variables standard"
    - "Les ARNs de la SFN et de la Lambda sont exportes dans root outputs.tf et dans all_step_function_arns"
    - "terraform fmt -check -recursive passe sans erreur sur le module sync/"
  artifacts:
    - path: "modules/step-functions/sync/main.tf"
      provides: "SFN + Lambda + IAM + CloudWatch + permissions"
      contains: "aws_sfn_state_machine"
    - path: "modules/step-functions/sync/variables.tf"
      provides: "Variables du module sync"
      contains: "variable \"prefix\""
    - path: "modules/step-functions/sync/outputs.tf"
      provides: "Outputs ARNs SFN et Lambda"
      contains: "step_function_arns"
    - path: "modules/step-functions/sync/versions.tf"
      provides: "Version constraints Terraform + providers"
      contains: "required_version"
    - path: "main.tf"
      provides: "Root wiring du module sync"
      contains: "module \"step_functions_sync\""
    - path: "outputs.tf"
      provides: "Root outputs pour sync SFN ARNs"
      contains: "step_functions_sync"
  key_links:
    - from: "modules/step-functions/sync/main.tf"
      to: "modules/step-functions/sync/sync_config_items.asl.json"
      via: "templatefile() avec SyncConfigItemsLambdaArn"
      pattern: "templatefile.*sync_config_items.asl.json"
    - from: "modules/step-functions/sync/main.tf"
      to: "lambdas/sync-config-items/sync_config_items.py"
      via: "archive_file source_file"
      pattern: "lambdas/sync-config-items/sync_config_items.py"
    - from: "main.tf"
      to: "modules/step-functions/sync"
      via: "module source"
      pattern: "module.*step_functions_sync"
    - from: "outputs.tf"
      to: "module.step_functions_sync"
      via: "output value reference"
      pattern: "module.step_functions_sync.step_function_arns"
---

<objective>
Creer le module Terraform modules/step-functions/sync/ qui deploie la SFN SyncConfigItems et la Lambda sync_config_items, puis wirer le module dans root main.tf et outputs.tf.

Purpose: L'infrastructure Terraform complete pour la sync config items, deployable via terraform plan/apply. Le module suit le pattern exact du module audit/ (Lambda inline via archive_file, SFN via for_each, IAM pour STS cross-account, CloudWatch logging).
Output: Module Terraform complet + wiring root + terraform fmt clean.
</objective>

<execution_context>
@/Users/clark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/clark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-foundation/04-CONTEXT.md
@.planning/phases/04-foundation/04-RESEARCH.md
@.planning/phases/04-foundation/04-01-SUMMARY.md

@modules/step-functions/audit/main.tf
@modules/step-functions/audit/variables.tf
@modules/step-functions/audit/outputs.tf
@modules/step-functions/audit/versions.tf
@main.tf
@outputs.tf
@variables.tf

<interfaces>
<!-- Pattern exact du module audit/ a reproduire pour sync/ -->

From modules/step-functions/audit/main.tf:
```hcl
locals {
  step_functions = {
    audit_resource = "audit_resource.asl.json"
  }
  sfn_names = {
    for k, v in local.step_functions : k => (
      var.naming_convention == "pascal"
      ? "${var.prefix}-Audit-${replace(title(replace(k, "_", " ")), " ", "")}"
      : "${var.prefix}-audit-${replace(k, "_", "-")}"
    )
  }
}
resource "aws_sfn_state_machine" "audit" { for_each = local.step_functions ... }
resource "aws_lambda_function" "cloudtrail_audit" { ... }
resource "aws_iam_role" "lambda_cloudtrail_audit" { ... }
resource "aws_iam_role_policy" "lambda_cloudtrail_audit" { ... }
resource "aws_lambda_permission" "sfn_invoke" { ... }
resource "aws_cloudwatch_log_group" "sfn" { count = var.enable_logging ? 1 : 0 ... }
resource "aws_cloudwatch_log_group" "lambda" { ... }
```

From modules/step-functions/audit/variables.tf:
```hcl
variable "prefix" {}
variable "orchestrator_role_arn" {}
variable "cross_account_role_arns" { type = list(string), default = [] }
variable "naming_convention" { default = "kebab" }
variable "enable_logging" { default = true }
variable "enable_xray_tracing" { default = false }
variable "log_retention_days" { default = 30 }
variable "log_level" { default = "INFO" }
variable "tags" { type = map(string), default = {} }
```

From modules/step-functions/audit/outputs.tf:
```hcl
output "step_function_arns" { value = { for k, v in aws_sfn_state_machine.audit : k => v.arn } }
output "step_function_names" { value = local.sfn_names }
output "audit_resource_arn" { value = aws_sfn_state_machine.audit["audit_resource"].arn }
output "lambda_function_arn" { value = aws_lambda_function.cloudtrail_audit.arn }
output "lambda_function_name" { value = aws_lambda_function.cloudtrail_audit.function_name }
output "lambda_role_arn" { value = aws_iam_role.lambda_cloudtrail_audit.arn }
```

From root main.tf (wiring pattern):
```hcl
module "step_functions_db" {
  source                = "./modules/step-functions/db"
  prefix                = var.prefix
  tags                  = var.tags
  orchestrator_role_arn = module.iam.orchestrator_role_arn
  enable_logging        = var.enable_step_functions_logging
  log_retention_days    = var.log_retention_days
  enable_xray_tracing   = var.enable_xray_tracing
}
```

From root outputs.tf (pattern):
```hcl
output "step_functions_db" {
  description = "Map of database Step Functions ARNs"
  value       = module.step_functions_db.step_function_arns
}
output "all_step_function_arns" {
  value = {
    db           = module.step_functions_db.step_function_arns
    efs          = module.step_functions_efs.step_function_arns
    eks          = module.step_functions_eks.step_function_arns
    utils        = module.step_functions_utils.step_function_arns
    orchestrator = module.orchestrator.step_function_arns
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Module Terraform sync/ (main.tf, variables.tf, outputs.tf, versions.tf)</name>
  <files>modules/step-functions/sync/main.tf, modules/step-functions/sync/variables.tf, modules/step-functions/sync/outputs.tf, modules/step-functions/sync/versions.tf</files>
  <read_first>
    modules/step-functions/audit/main.tf
    modules/step-functions/audit/variables.tf
    modules/step-functions/audit/outputs.tf
    modules/step-functions/audit/versions.tf
    modules/step-functions/sync/sync_config_items.asl.json
    .planning/phases/04-foundation/04-RESEARCH.md
  </read_first>
  <action>
    Creer les 4 fichiers du module sync/ en copiant le pattern EXACT du module audit/ avec les adaptations suivantes :

    **versions.tf** : Identique a audit/versions.tf (terraform >= 1.0, aws >= 5.0, archive >= 2.0)

    **variables.tf** : Identique a audit/variables.tf avec les memes variables (prefix, orchestrator_role_arn, cross_account_role_arns, naming_convention, enable_logging, enable_xray_tracing, log_retention_days, log_level, tags). Aucune variable supplementaire necessaire pour la Phase 4.

    **main.tf** : Reproduire le pattern audit/ avec ces adaptations :
    1. Header comment: "Sync Step Functions Module — Step Functions for syncing config items (secrets/parameters) between AWS accounts"
    2. locals.step_functions: `{ sync_config_items = "sync_config_items.asl.json" }`
    3. locals.sfn_names: Meme pattern que audit/ mais avec "Sync" (pascal) / "sync" (kebab) au lieu de "Audit" / "audit"
    4. data.archive_file.sync_config_items: source_file = "${path.module}/../../../lambdas/sync-config-items/sync_config_items.py", output_path = "${path.module}/../../../lambdas/sync-config-items.zip"
    5. aws_lambda_function.sync_config_items: function_name = "${var.prefix}-sync-config-items", description = "Sync config items (secrets/parameters) between AWS accounts", handler = "sync_config_items.lambda_handler", runtime = "python3.12", timeout = 300 (5 minutes -- plus long que audit car les operations SM/SSM sont plus lentes), tags Module = "sync"
    6. aws_iam_role.lambda_sync_config_items: name = "${var.prefix}-lambda-sync-config-items", assume_role_policy identique (lambda.amazonaws.com)
    7. aws_iam_role_policy.lambda_sync_config_items: Policy avec SEULEMENT 2 statements :
       - "AssumeRoleForCrossAccount": sts:AssumeRole sur var.cross_account_role_arns
       - "CloudWatchLogs": logs:CreateLogGroup/CreateLogStream/PutLogEvents sur arn:aws:logs:*:*:*
       IMPORTANT: PAS de permissions secretsmanager:* ou ssm:* -- les permissions SM/SSM sont sur les roles cross-account assumes (voir RESEARCH.md Pitfall 2)
    8. aws_sfn_state_machine.sync: for_each local.step_functions, templatefile avec { SyncConfigItemsLambdaArn = aws_lambda_function.sync_config_items.arn }
    9. aws_lambda_permission.sfn_invoke: source_arn = aws_sfn_state_machine.sync["sync_config_items"].arn
    10. aws_cloudwatch_log_group.sfn: name = "/aws/stepfunctions/${var.prefix}-sync"
    11. aws_cloudwatch_log_group.lambda: name = "/aws/lambda/${aws_lambda_function.sync_config_items.function_name}"

    **outputs.tf** : Meme pattern que audit/ avec adaptations :
    - step_function_arns: { for k, v in aws_sfn_state_machine.sync : k => v.arn }
    - step_function_names: local.sfn_names
    - sync_config_items_arn: aws_sfn_state_machine.sync["sync_config_items"].arn
    - lambda_function_arn: aws_lambda_function.sync_config_items.arn
    - lambda_function_name: aws_lambda_function.sync_config_items.function_name
    - lambda_role_arn: aws_iam_role.lambda_sync_config_items.arn

    Apres creation, lancer `terraform fmt -recursive modules/step-functions/sync/` pour formater.
  </action>
  <verify>
    <automated>terraform fmt -check -recursive modules/step-functions/sync/</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "aws_sfn_state_machine" modules/step-functions/sync/main.tf
    - grep -q "aws_lambda_function" modules/step-functions/sync/main.tf
    - grep -q "aws_iam_role" modules/step-functions/sync/main.tf
    - grep -q "aws_lambda_permission" modules/step-functions/sync/main.tf
    - grep -q "aws_cloudwatch_log_group" modules/step-functions/sync/main.tf
    - grep -q "SyncConfigItemsLambdaArn" modules/step-functions/sync/main.tf
    - grep -q "archive_file" modules/step-functions/sync/main.tf
    - grep -q "sync_config_items.py" modules/step-functions/sync/main.tf
    - grep -q "sts:AssumeRole" modules/step-functions/sync/main.tf
    - grep -qE "secretsmanager|ssm:" modules/step-functions/sync/main.tf && echo "FAIL: SM/SSM permissions on Lambda role" || echo "OK: no SM/SSM permissions on Lambda role"
    - grep -q "variable \"prefix\"" modules/step-functions/sync/variables.tf
    - grep -q "variable \"orchestrator_role_arn\"" modules/step-functions/sync/variables.tf
    - grep -q "variable \"cross_account_role_arns\"" modules/step-functions/sync/variables.tf
    - grep -q "step_function_arns" modules/step-functions/sync/outputs.tf
    - grep -q "lambda_function_arn" modules/step-functions/sync/outputs.tf
    - grep -q "required_version" modules/step-functions/sync/versions.tf
    - terraform fmt -check -recursive modules/step-functions/sync/ retourne 0
  </acceptance_criteria>
  <done>Module Terraform sync/ complet avec main.tf (SFN + Lambda inline + IAM STS-only + CloudWatch + permission), variables.tf, outputs.tf, versions.tf -- tout formate correctement</done>
</task>

<task type="auto">
  <name>Task 2: Wiring root main.tf + outputs.tf</name>
  <files>main.tf, outputs.tf</files>
  <read_first>
    main.tf
    outputs.tf
    variables.tf
    modules/step-functions/sync/variables.tf
    modules/step-functions/sync/outputs.tf
  </read_first>
  <action>
    **main.tf** : Ajouter un nouveau bloc module apres le bloc "step_functions_utils" (avant le bloc IAM) :

    ```hcl
    # -----------------------------------------------------------------------------
    # Step Functions - Sync Module
    # -----------------------------------------------------------------------------
    module "step_functions_sync" {
      source = "./modules/step-functions/sync"

      prefix                = var.prefix
      tags                  = var.tags
      orchestrator_role_arn = module.iam.orchestrator_role_arn

      cross_account_role_arns = concat(var.source_role_arns, var.destination_role_arns)

      enable_logging      = var.enable_step_functions_logging
      log_retention_days  = var.log_retention_days
      enable_xray_tracing = var.enable_xray_tracing
    }
    ```

    Points importants :
    - cross_account_role_arns recoit la concatenation des source et destination role ARNs -- la Lambda sync a besoin d'assumer les roles dans les DEUX comptes (lire source + ecrire destination)
    - Memes variables standard que les autres modules (prefix, tags, orchestrator_role_arn, enable_logging, etc.)
    - Positionner le bloc dans l'ordre logique avec les autres modules step-functions

    **outputs.tf** : Ajouter 2 modifications :

    1. Nouveau output bloc (apres step_functions_utils) :
    ```hcl
    # -----------------------------------------------------------------------------
    # Step Functions - Sync Outputs
    # -----------------------------------------------------------------------------

    output "step_functions_sync" {
      description = "Map of Sync Step Functions ARNs"
      value       = module.step_functions_sync.step_function_arns
    }
    ```

    2. Ajouter dans all_step_function_arns la ligne `sync = module.step_functions_sync.step_function_arns` (apres la ligne utils) :
    ```hcl
    output "all_step_function_arns" {
      description = "Consolidated map of all Step Function ARNs by module"
      value = {
        db           = module.step_functions_db.step_function_arns
        efs          = module.step_functions_efs.step_function_arns
        eks          = module.step_functions_eks.step_function_arns
        utils        = module.step_functions_utils.step_function_arns
        sync         = module.step_functions_sync.step_function_arns
        orchestrator = module.orchestrator.step_function_arns
      }
    }
    ```

    Apres modification, lancer `terraform fmt main.tf outputs.tf` pour formater.
  </action>
  <verify>
    <automated>terraform fmt -check main.tf outputs.tf</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'module "step_functions_sync"' main.tf
    - grep -q './modules/step-functions/sync' main.tf
    - grep -q 'module.iam.orchestrator_role_arn' main.tf
    - grep -q 'cross_account_role_arns' main.tf
    - grep -q 'step_functions_sync' outputs.tf
    - grep -q 'module.step_functions_sync.step_function_arns' outputs.tf
    - grep -q 'sync.*=.*module.step_functions_sync' outputs.tf
    - terraform fmt -check main.tf outputs.tf retourne 0
  </acceptance_criteria>
  <done>Module sync wire dans root main.tf avec les memes variables que les autres modules step-functions, ARNs exportes dans outputs.tf et all_step_function_arns consolide</done>
</task>

</tasks>

<verification>
- terraform fmt -check -recursive modules/step-functions/sync/ -- format OK
- terraform fmt -check main.tf outputs.tf -- format OK
- Le module sync/ contient exactement 5 fichiers : main.tf, variables.tf, outputs.tf, versions.tf, sync_config_items.asl.json
- main.tf root contient module "step_functions_sync" avec source ./modules/step-functions/sync
- outputs.tf root contient sync dans all_step_function_arns
- La policy IAM Lambda ne contient PAS de permissions secretsmanager ou ssm (seulement sts:AssumeRole + CloudWatch Logs)
</verification>

<success_criteria>
- Module Terraform complet et formate
- Root main.tf wire le module avec cross_account_role_arns
- Root outputs.tf exporte les ARNs sync
- all_step_function_arns inclut la cle sync
- Zero permissions SM/SSM sur le role Lambda (seulement STS + CloudWatch)
</success_criteria>

<output>
After completion, create `.planning/phases/04-foundation/04-02-SUMMARY.md`
</output>
