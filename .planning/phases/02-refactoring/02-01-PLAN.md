---
phase: 02-refactoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/step-functions/efs/check_flag_file_sync.asl.json
  - modules/step-functions/efs/check_replication_sync.asl.json
  - modules/step-functions/efs/setup_cross_account_replication.asl.json
  - modules/step-functions/efs/main.tf
  - modules/step-functions/efs/outputs.tf
  - modules/step-functions/efs/README.md
  - tests/test_interface_snapshots.py
  - tests/test_asl_validation.py
  - tests/snapshots/check_replication_sync_outputs.json
  - tests/snapshots/setup_cross_account_replication_outputs.json
autonomous: true
requirements: [REF-01, REF-02, REF-05]

must_haves:
  truths:
    - "check_replication_sync.asl.json has ~35 states (down from 72) and calls ManageLambdaLifecycle x2, ManageAccessPoint x2, and CheckFlagFileSync via startExecution.sync:2"
    - "setup_cross_account_replication.asl.json has ~39-41 states (down from 53) and calls ManageFileSystemPolicy x2 via startExecution.sync:2"
    - "check_flag_file_sync.asl.json exists as a new sub-SFN following Phase 1 patterns (Comment I/O contract, named Fail, self-contained Catch)"
    - "All $$.Execution.Input references in check_replication_sync are materialized into state data before sub-SFN calls"
    - "Interface snapshots captured BEFORE refactoring and validated AFTER -- output schemas unchanged (REF-05)"
    - "EFS module uses dual resource maps (file() + templatefile()) with moved blocks for zero-downtime migration"
  artifacts:
    - path: "modules/step-functions/efs/check_flag_file_sync.asl.json"
      provides: "CheckFlagFileSync sub-SFN"
      contains: "CheckFlagFileSyncFailed"
    - path: "modules/step-functions/efs/check_replication_sync.asl.json"
      provides: "Refactored check_replication_sync calling sub-SFNs"
      contains: "manage_lambda_lifecycle_arn"
    - path: "modules/step-functions/efs/setup_cross_account_replication.asl.json"
      provides: "Refactored setup_cross_account_replication calling ManageFileSystemPolicy"
      contains: "manage_filesystem_policy_arn"
    - path: "modules/step-functions/efs/main.tf"
      provides: "Dual resource maps with moved blocks"
      contains: "step_functions_templated"
    - path: "modules/step-functions/efs/outputs.tf"
      provides: "Merged output map from both resources"
      contains: "merge("
    - path: "tests/test_interface_snapshots.py"
      provides: "Interface non-regression tests (REF-05)"
      contains: "extract_output_schema"
    - path: "tests/snapshots/check_replication_sync_outputs.json"
      provides: "Pre-refactoring output schema reference"
    - path: "tests/snapshots/setup_cross_account_replication_outputs.json"
      provides: "Pre-refactoring output schema reference"
  key_links:
    - from: "modules/step-functions/efs/main.tf"
      to: "modules/step-functions/efs/check_replication_sync.asl.json"
      via: "templatefile() with ARN injection"
      pattern: "templatefile.*check_replication_sync"
    - from: "modules/step-functions/efs/check_replication_sync.asl.json"
      to: "ManageLambdaLifecycle, ManageAccessPoint, CheckFlagFileSync sub-SFNs"
      via: "states:startExecution.sync:2 with ResultSelector"
      pattern: "startExecution\\.sync:2"
    - from: "modules/step-functions/efs/main.tf"
      to: "modules/step-functions/efs/outputs.tf"
      via: "merge() of efs + efs_templated resource maps"
      pattern: "merge.*efs.*efs_templated"
    - from: "modules/step-functions/efs/main.tf"
      to: "moved blocks"
      via: "Declarative resource address migration"
      pattern: "moved.*from.*to"
---

<objective>
Refactor the two most complex EFS ASL files (check_replication_sync 72->~35 states, setup_cross_account_replication 53->~39 states) by replacing inline duplicated blocks with calls to Phase 1 sub-SFNs and a new CheckFlagFileSync sub-SFN. Migrate these two files from file() to templatefile() in Terraform using dual resource maps and moved blocks.

Purpose: Eliminate the largest source of inline duplication in the project (Lambda lifecycle x2, access point x2, flag file sync, filesystem policy x2) while maintaining identical external interfaces.
Output: Refactored ASL files, new CheckFlagFileSync sub-SFN, dual-map Terraform migration, interface snapshot tests
</objective>

<execution_context>
@/Users/clark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/clark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-refactoring/02-RESEARCH.md
@.planning/phases/02-refactoring/02-CONTEXT.md
@.planning/phases/01-extraction/01-02-SUMMARY.md
@.planning/phases/01-extraction/01-03-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs from Phase 1 sub-SFNs -->

From modules/step-functions/efs/README.md:

ManageLambdaLifecycle (8 states):
  Input: {LambdaConfig: {FunctionName, Runtime, Handler, Role, Code, Timeout?, MemorySize?, Architectures?, VpcConfig?, Environment?, Tags?, ForceUpdateCode?}, Account: {RoleArn}}
  Output: {FunctionName, FunctionArn, Status: "Ready"}
  Error: "ManageLambdaLifecycleFailed"

ManageAccessPoint (6 states):
  Input: {FileSystemId, AccessPointConfig: {ClientToken, PosixUser?, RootDirectory?, Tags?}, Account: {RoleArn}}
  Output: {AccessPointId, AccessPointArn, FileSystemId, Status: "Available"}
  Error: "ManageAccessPointFailed"

ManageFileSystemPolicy (12 states):
  Input: {FileSystemId, Account: {RoleArn}, Action: "ADD"|"REMOVE", PolicyStatement: {Sid, ...}}
  Output: {FileSystemId, PolicyApplied: true, Status: "Completed"}
  Error: "ManageFileSystemPolicyFailed"

From modules/step-functions/efs/main.tf:
  local.step_functions map with for_each -> aws_sfn_state_machine.efs
  local.sfn_names uses naming_convention (pascal/kebab)

From modules/step-functions/efs/outputs.tf:
  output "step_function_arns" = { for k, v in aws_sfn_state_machine.efs : k => v.arn }
  output "step_function_names" = { for k, v in aws_sfn_state_machine.efs : k => v.name }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create interface snapshot tests + capture pre-refactoring baselines + create CheckFlagFileSync sub-SFN</name>
  <files>tests/test_interface_snapshots.py, tests/snapshots/check_replication_sync_outputs.json, tests/snapshots/setup_cross_account_replication_outputs.json, modules/step-functions/efs/check_flag_file_sync.asl.json, tests/test_asl_validation.py</files>
  <action>
**Step 1: Create tests/test_interface_snapshots.py** implementing the interface non-regression test for REF-05.
- Extract output schema from ASL files by finding terminal Pass states (states whose Next is a Succeed state, or Succeed states themselves) and capturing their Parameters/Output keys.
- Also extract the Comment field (which documents Input/Output contract per Phase 1 pattern).
- Write parametrized tests for: check_replication_sync, setup_cross_account_replication, prepare_snapshot_for_restore, restore_cluster, refresh_orchestrator (all 5 files being refactored across all plans).
- On first run, if snapshot file does not exist, create it (bootstrap mode).
- On subsequent runs, assert current schema matches snapshot exactly.
- Use `tests/snapshots/` directory for reference files.

**Step 2: Run the test in bootstrap mode** to capture pre-refactoring output schemas:
```bash
mkdir -p tests/snapshots
python -m pytest tests/test_interface_snapshots.py -x -q
```
This creates the snapshot JSON files that will serve as the reference BEFORE any refactoring.

**Step 3: Create check_flag_file_sync.asl.json** in `modules/step-functions/efs/`.
- Follow Phase 1 sub-SFN patterns: Comment with I/O contract, named Fail state (CheckFlagFileSyncFailed), self-contained Catch on all Task states.
- Input: {SourceFileSystemId, DestinationFileSystemId, SourceAccount: {RoleArn}, DestinationAccount: {RoleArn}, LambdaConfig: {Source*, Destination*, Code, Tags, ForceUpdateCode}, SourceSubpath, DestinationSubpath, FlagId}
- Output: {SyncVerified: bool, FlagId, SourceTime, DestinationTime, Status}
- This sub-SFN calls ManageLambdaLifecycle x2 and ManageAccessPoint x2 as sub-sub-SFNs via startExecution.sync:2 (3-level nesting accepted per research -- 10s overhead is negligible vs 4h max runtime).
- Use templatefile() variables for the sub-SFN ARNs: ${manage_lambda_lifecycle_arn}, ${manage_access_point_arn}.
- Sequence: EnsureSourceLambda -> CreateSourceAP -> BindSourceAPToLambda -> EnsureDestLambda -> CreateDestAP -> BindDestAPToLambda -> WriteFlagFile -> WaitPropagation -> CheckFlagLoop (poll) -> CleanupAccessPoints -> PrepareOutput -> SyncVerified (Succeed) / CheckFlagFileSyncFailed (Fail).
- Use discriminators in execution names: States.Format('{}-SrcLambda', $$.Execution.Name), States.Format('{}-DstLambda', $$.Execution.Name), States.Format('{}-SrcAP', $$.Execution.Name), States.Format('{}-DstAP', $$.Execution.Name).
- Each sub-SFN call uses ResultSelector to extract $.Output.* fields (the .sync:2 output envelope).

**Step 4: Update tests/test_asl_validation.py** -- expand `TestASLCatchSelfContained._get_sub_sfn_files()` to also discover sub-SFN files from DB and orchestrator modules (not just `efs/manage_*.asl.json`). Use a list of glob patterns:
- `efs/manage_*.asl.json` (existing)
- `efs/check_flag_file_sync.asl.json` (new Phase 2)
- `db/ensure_snapshot_available.asl.json` (future Plan 02-02)
- `db/cluster_switch_sequence.asl.json` (future Plan 02-03)
Pattern: scan for files matching known sub-SFN patterns across modules. Use a list of (module_dir, glob_pattern) tuples and skip files that don't exist yet.
  </action>
  <verify>
    <automated>python -m pytest tests/test_interface_snapshots.py tests/test_asl_validation.py -x -q</automated>
  </verify>
  <done>Interface snapshot files exist in tests/snapshots/ for all 5 refactored SFNs. check_flag_file_sync.asl.json exists with ~21 states, Comment I/O contract, named Fail state, self-contained Catch. TestASLCatchSelfContained discovers sub-SFN files across modules. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor check_replication_sync.asl.json -- replace inline blocks with sub-SFN calls</name>
  <files>modules/step-functions/efs/check_replication_sync.asl.json</files>
  <action>
**CRITICAL FIRST STEP: Audit $$.Execution.Input references.**
Read check_replication_sync.asl.json and identify all `$$.Execution.Input` references (3 known: lines ~31, 50, 51 for SSM parameter names). Also audit SSM PutParameter/GetParameter states that consume these values.

**Step 1: Materialize $$.Execution.Input** into the state data.
In the existing InitializeState (or a new dedicated Pass state right after), copy SSM parameter names from `$$.Execution.Input` into the regular state data:
- `SourceSubpathSSMParameter` (used in isPresent check + value read)
- `DestinationSubpathSSMParameter` (used in value read with fallback)
After this, ALL references to `$$.Execution.Input` can be replaced with regular `$.SourceSubpathSSMParameter` / `$.DestinationSubpathSSMParameter`.

**Step 2: Replace inline Lambda lifecycle blocks** with calls to ManageLambdaLifecycle sub-SFN.
- Source Lambda lifecycle (~6 states: check exists, create/handle race, wait, update code) -> 1 Task state calling ManageLambdaLifecycle via startExecution.sync:2 with `"StateMachineArn": "${manage_lambda_lifecycle_arn}"`.
- Destination Lambda lifecycle (~6 states: same pattern) -> 1 Task state.
- Use discriminators: `States.Format('{}-SrcLambda', $$.Execution.Name)` and `States.Format('{}-DstLambda', $$.Execution.Name)`.
- Use ResultSelector to extract `$.Output.FunctionName`, `$.Output.FunctionArn`, `$.Output.Status`.

**Step 3: Replace inline access point blocks** with calls to ManageAccessPoint sub-SFN.
- Source AP creation + wait loop (~4 states) -> 1 Task state calling ManageAccessPoint.
- Destination AP creation + wait loop (~4 states) -> 1 Task state.
- Discriminators: `States.Format('{}-SrcAP', $$.Execution.Name)`, `States.Format('{}-DstAP', $$.Execution.Name)`.
- ResultSelector: `$.Output.AccessPointId`, `$.Output.AccessPointArn`, `$.Output.Status`.

**Step 4: Replace inline flag file sync** with a single call to CheckFlagFileSync sub-SFN.
- The entire flag write -> wait -> poll -> verify block (~20+ states) -> 1 Task state calling CheckFlagFileSync via `"StateMachineArn": "${check_flag_file_sync_arn}"`.
- Pass all needed context (FS IDs, Lambda configs, subpaths, FlagId) as Input.
- ResultSelector: `$.Output.SyncVerified`, `$.Output.FlagId`, `$.Output.Status`.

**Step 5: Preserve all remaining states** -- SSM reads, cleanup (delete AP, delete Lambda), error handling, and the final output Pass state must remain identical. The output schema (Parameters of the terminal Pass state) MUST NOT change.

**Step 6: Add templatefile() variables** -- the file must use `${manage_lambda_lifecycle_arn}`, `${manage_access_point_arn}`, `${check_flag_file_sync_arn}` as template variables for StateMachineArn values. These are injected by Terraform templatefile() in main.tf.

**Target: ~35 states** (down from 72). The exact count depends on cleanup states that remain.

**CRITICAL: Do NOT change the output schema.** The final Pass/Succeed state must produce the same keys as the pre-refactoring version captured in tests/snapshots/.
  </action>
  <verify>
    <automated>python -m pytest tests/test_asl_validation.py -k "check_replication_sync" -x -q && python -m pytest tests/test_interface_snapshots.py -k "check_replication_sync" -x -q</automated>
  </verify>
  <done>check_replication_sync.asl.json has ~35 states (down from 72). All $$.Execution.Input references materialized. Sub-SFN calls use startExecution.sync:2 with discriminated execution names. Output schema unchanged (interface snapshot test passes). ASL validation passes (valid JSON, reachable states, Catch/Next valid).</done>
</task>

<task type="auto">
  <name>Task 3: Refactor setup_cross_account_replication.asl.json + Terraform dual-map migration + update outputs and README</name>
  <files>modules/step-functions/efs/setup_cross_account_replication.asl.json, modules/step-functions/efs/main.tf, modules/step-functions/efs/outputs.tf, modules/step-functions/efs/README.md</files>
  <action>
**Step 1: Refactor setup_cross_account_replication.asl.json.**
Replace the 2 inline filesystem policy management sequences with calls to ManageFileSystemPolicy sub-SFN:
- Source FS policy ADD (~8 states: get policy, parse, merge, put) -> 1 Task state calling ManageFileSystemPolicy with Action=ADD.
- Destination FS policy ADD (~8 states: same pattern) -> 1 Task state.
- Use discriminators: `States.Format('{}-SrcPolicy', $$.Execution.Name)`, `States.Format('{}-DstPolicy', $$.Execution.Name)`.
- StateMachineArn: `${manage_filesystem_policy_arn}` (templatefile variable).
- ResultSelector: `$.Output.FileSystemId`, `$.Output.PolicyApplied`, `$.Output.Status`.
- Preserve all cross-region proxy routing states (these are specific to this SFN, not duplicated).
- Preserve the replication configuration states, describe replication states, and final output.
- **Target: ~39-41 states** (removing 16 policy states + adding 2 sub-SFN calls). The ~30 target from ROADMAP uses tilde approximation.
- **CRITICAL: Do NOT change the output schema.** Interface snapshot test must pass.
- Add templatefile() variable `${manage_filesystem_policy_arn}` for StateMachineArn values.

**Step 2: Update modules/step-functions/efs/main.tf** with dual resource maps.
- Remove `check_replication_sync` and `setup_cross_account_replication` from `local.step_functions`.
- Add `check_flag_file_sync` to `local.step_functions` (new sub-SFN, uses file()).
- Create new `local.step_functions_templated` map with `check_replication_sync` and `setup_cross_account_replication`.
- Update `local.sfn_names` to use `merge(local.step_functions, local.step_functions_templated)` so names cover BOTH maps.
- Create new `resource "aws_sfn_state_machine" "efs_templated"` with:
  - `for_each = local.step_functions_templated`
  - `definition = templatefile("${path.module}/${each.value}", { ... })` with ARN variables:
    - `manage_lambda_lifecycle_arn = aws_sfn_state_machine.efs["manage_lambda_lifecycle"].arn`
    - `manage_access_point_arn = aws_sfn_state_machine.efs["manage_access_point"].arn`
    - `manage_filesystem_policy_arn = aws_sfn_state_machine.efs["manage_filesystem_policy"].arn`
    - `check_flag_file_sync_arn = aws_sfn_state_machine.efs["check_flag_file_sync"].arn`
  - Same logging_configuration, tracing_configuration, tags as existing resource.
- Add moved blocks AFTER both resources:
  ```
  moved { from = aws_sfn_state_machine.efs["check_replication_sync"] to = aws_sfn_state_machine.efs_templated["check_replication_sync"] }
  moved { from = aws_sfn_state_machine.efs["setup_cross_account_replication"] to = aws_sfn_state_machine.efs_templated["setup_cross_account_replication"] }
  ```
- NOTE: check_flag_file_sync itself uses templatefile() for sub-SFN ARN injection (it calls ManageLambdaLifecycle and ManageAccessPoint). So check_flag_file_sync should ALSO be in `step_functions_templated`, not `step_functions`. Adjust accordingly: the sub-SFN calls sub-sub-SFNs, so it needs ARN injection too. However there is no moved block needed since it is a new file.

**Step 3: Update modules/step-functions/efs/outputs.tf.**
- Change `step_function_arns` to merge both resource maps:
  ```
  value = merge(
    { for k, v in aws_sfn_state_machine.efs : k => v.arn },
    { for k, v in aws_sfn_state_machine.efs_templated : k => v.arn }
  )
  ```
- Same for `step_function_names`:
  ```
  value = merge(
    { for k, v in aws_sfn_state_machine.efs : k => v.name },
    { for k, v in aws_sfn_state_machine.efs_templated : k => v.name }
  )
  ```

**Step 4: Update modules/step-functions/efs/README.md.**
- Add CheckFlagFileSync section following same format as existing sub-SFNs (Input, Output, Behavior, Catch).
- Update Integration Pattern section to show the dual-map approach.
- Add check_flag_file_sync to the Naming table.
  </action>
  <verify>
    <automated>python -m pytest tests/test_asl_validation.py -k "setup_cross_account_replication or check_flag_file_sync" -x -q && python -m pytest tests/test_interface_snapshots.py -k "setup_cross_account_replication" -x -q</automated>
  </verify>
  <done>setup_cross_account_replication.asl.json has ~39-41 states (down from 53). ManageFileSystemPolicy called x2 via sub-SFN. main.tf has dual resource maps with moved blocks. outputs.tf merges both maps. README documents CheckFlagFileSync. Interface snapshot test passes. All ASL validation tests pass.</done>
</task>

</tasks>

<verification>
After all 3 tasks:

1. **Full test suite green:**
   ```bash
   python -m pytest tests/ -x -q
   ```

2. **State count validation:**
   ```bash
   python3 -c "import json; d=json.load(open('modules/step-functions/efs/check_replication_sync.asl.json')); print(f'check_replication_sync: {len(d[\"States\"])} states')"
   python3 -c "import json; d=json.load(open('modules/step-functions/efs/setup_cross_account_replication.asl.json')); print(f'setup_cross_account_replication: {len(d[\"States\"])} states')"
   python3 -c "import json; d=json.load(open('modules/step-functions/efs/check_flag_file_sync.asl.json')); print(f'check_flag_file_sync: {len(d[\"States\"])} states')"
   ```

3. **No $$.Execution.Input references remain** in check_replication_sync (all materialized):
   ```bash
   grep -c 'Execution.Input' modules/step-functions/efs/check_replication_sync.asl.json
   ```
   Expected: 0 (or only in InitializeState materialization)

4. **Interface snapshots match** (REF-05):
   ```bash
   python -m pytest tests/test_interface_snapshots.py -x -q
   ```

5. **Terraform syntax valid** (HCL parsing only, no state/provider needed):
   ```bash
   cd modules/step-functions/efs && terraform fmt -check && cd ../../..
   ```
</verification>

<success_criteria>
- check_replication_sync.asl.json reduced from 72 to ~35 states
- setup_cross_account_replication.asl.json reduced from 53 to ~39-41 states
- check_flag_file_sync.asl.json exists as a new sub-SFN (~21 states) with Phase 1 patterns
- EFS main.tf has dual resource maps (step_functions + step_functions_templated) with moved blocks
- outputs.tf merges both maps into single output
- Interface snapshot tests pass for both refactored files (REF-05)
- All ASL validation tests pass
- No remaining $$.Execution.Input references outside materialization state
</success_criteria>

<output>
After completion, create `.planning/phases/02-refactoring/02-01-SUMMARY.md`
</output>
