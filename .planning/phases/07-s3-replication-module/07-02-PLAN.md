---
phase: 07-s3-replication-module
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/step-functions/s3/run_batch_replication.asl.json
  - modules/step-functions/s3/check_batch_replication.asl.json
autonomous: true
requirements: [REPL-02, REPL-03, REPL-05, REPL-06]
must_haves:
  truths:
    - "run_batch ASL creates an S3 Batch Operations job (S3ReplicateObject) reusing the live replication config, backfilling all destinations in one job"
    - "run_batch ASL uses GeneratedManifest (no S3 Inventory precondition) filtering NONE/FAILED objects"
    - "run_batch ASL supplies a fresh ClientRequestToken via States.UUID() and ConfirmationRequired:false"
    - "check_batch ASL polls describeJob in a Wait+Task+Choice loop, terminating on Complete/Failed/Cancelled"
    - "both batch SFN assume the source role via Credentials.RoleArn.$ and pass AccountId explicitly"
    - "both ASL files are structurally valid (pytest passes)"
  artifacts:
    - path: "modules/step-functions/s3/run_batch_replication.asl.json"
      provides: "S3 Batch Replication job dispatch returning JobId"
      contains: "createJob"
    - path: "modules/step-functions/s3/check_batch_replication.asl.json"
      provides: "describeJob polling loop to completion"
      contains: "describeJob"
  key_links:
    - from: "run_batch_replication.asl.json"
      to: "S3 Batch Operations control plane"
      via: "aws-sdk:s3control:createJob with Operation S3ReplicateObject"
      pattern: "aws-sdk:s3control:createJob"
    - from: "check_batch_replication.asl.json"
      to: "S3 Batch Operations control plane"
      via: "aws-sdk:s3control:describeJob polling on Job.Status"
      pattern: "aws-sdk:s3control:describeJob"
---

<objective>
Author the two S3 Batch Operations ASL state machines of `modules/step-functions/s3/`: `run_batch_replication` (REPL-02) creating an `s3control:createJob` with `S3ReplicateObject` + GeneratedManifest to backfill existing objects, and `check_batch_replication` (REPL-03) polling `s3control:describeJob` to completion. Both use native SDK integrations only — NO Lambda (REPL-06) — and assume the source role imperatively.

Purpose: Live replication only forwards new writes; existing objects need a backfill. S3 Batch Replication reuses the `ReplicationConfiguration` posted by setup and replicates to ALL configured destinations in a single job (REPL-05), with S3 generating the manifest on demand (no inventory precondition).
Output: 2 valid `*.asl.json` files using the verified s3control request shapes from RESEARCH Patterns 2-3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-s3-replication-module/07-CONTEXT.md
@.planning/phases/07-s3-replication-module/07-RESEARCH.md
@.planning/phases/07-s3-replication-module/07-PATTERNS.md

<interfaces>
<!-- Verified s3control request shapes (RESEARCH Patterns 2-3). camelCase action, PascalCase params. -->
arn:aws:states:::aws-sdk:s3control:createJob
  Required params: AccountId, ClientRequestToken (1-64 chars, States.UUID()), Operation, Priority, Report, RoleArn
  Operation: { "S3ReplicateObject": {} }
  ManifestGenerator: { "S3JobManifestGenerator": { ExpectedBucketOwner, SourceBucket (ARN), EnableManifestOutput:false, Filter:{ EligibleForReplication:true, ObjectReplicationStatuses:["NONE","FAILED"] } } }
  Report: { Enabled, Bucket (ARN, when Enabled), Prefix, Format:"Report_CSV_20180820", ReportScope:"AllTasks" }
  ConfirmationRequired: false
  Response: { "JobId": "<id>" }   (Manifest and ManifestGenerator are mutually exclusive — use ManifestGenerator)

arn:aws:states:::aws-sdk:s3control:describeJob
  Params: AccountId, JobId
  Response: Job.Status enum: New|Preparing|Suspended|Ready|Active|Pausing|Paused|Complete|Cancelling|Cancelled|Failing|Failed
  Terminal: Complete (success), Failed/Cancelled (failure)

Imperative assume-role on every Task: "Credentials": { "RoleArn.$": "$.SourceAccount.RoleArn" }

Input contract (D-11):
  $.SourceAccount.AccountId, $.SourceAccount.RoleArn
  $.SourceBucketArn        (bucket ARN for ManifestGenerator.SourceBucket and ExpectedBucketOwner context)
  $.BatchReplicationRoleArn (the s3_replication role ARN passed as createJob RoleArn)
  $.ReportBucketArn        (OPTIONAL — when absent, emit Report:{Enabled:false}; O1)
  run_batch output: $.BatchJob.JobId  (consumed by check_batch via $.BatchJob.JobId)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: run_batch_replication.asl.json (createJob + GeneratedManifest)</name>
  <read_first>
    - modules/step-functions/efs/setup_cross_account_replication.asl.json (Task error-handling shell only: Credentials line 51-53, ResultSelector/ResultPath lines 54-58, Catch->Fail lines 60-66 — there is NO EFS analog for s3control, copy only the shell)
    - .planning/phases/07-s3-replication-module/07-RESEARCH.md (Pattern 2 lines 213-262: full verified createJob Parameters block; Pitfall 5 line 340 — same-region/AccountId/ExpectedBucketOwner; Open Question O1 line 432 — report bucket optional; Anti-Patterns line 294 — ClientRequestToken/ARN-not-name)
    - .planning/phases/07-s3-replication-module/07-PATTERNS.md (run_batch section lines 222-237)
  </read_first>
  <action>
    Create the file with "StartAt": "ShouldEnableReport". State flow:
    (1) `ShouldEnableReport` Choice: if `$.ReportBucketArn` `IsPresent` true -> `CreateBatchJobWithReport`; Default -> `CreateBatchJobNoReport` (O1: generic module owns no bucket).
    (2) `CreateBatchJobWithReport` Task -> `arn:aws:states:::aws-sdk:s3control:createJob`. Parameters per RESEARCH Pattern 2: `AccountId.$ = $.SourceAccount.AccountId`, `ConfirmationRequired: false`, `Priority: 1`, `ClientRequestToken.$ = States.UUID()`, `RoleArn.$ = $.BatchReplicationRoleArn`, `Operation: {"S3ReplicateObject": {}}`, `Report: {Enabled:true, "Bucket.$":"$.ReportBucketArn", Prefix:"batch-replication-report", Format:"Report_CSV_20180820", ReportScope:"AllTasks"}`, `ManifestGenerator: {"S3JobManifestGenerator": {"ExpectedBucketOwner.$":"$.SourceAccount.AccountId", "SourceBucket.$":"$.SourceBucketArn", EnableManifestOutput:false, Filter:{EligibleForReplication:true, ObjectReplicationStatuses:["NONE","FAILED"]}}}`. `Credentials.RoleArn.$ = $.SourceAccount.RoleArn`. `ResultSelector: {"JobId.$":"$.JobId"}`, ResultPath `$.BatchJob`, Next `PrepareOutput`, Catch `States.ALL` -> `BatchJobFailed`.
    (3) `CreateBatchJobNoReport` Task: identical but `Report: {Enabled:false}` (still required by the API) and no Report.Bucket.
    (4) `PrepareOutput` Pass (Status "Completed", ModuleName "run_batch_replication", pass through `$.BatchJob.JobId`) -> `BatchSucceeded` Succeed.
    (5) `BatchJobFailed` Fail, Error "BatchReplicationJobFailed". Pass bucket ARNs (not names) for SourceBucket/Report.Bucket. Same-region only: do not add region routing. No lambda:invoke.
  </action>
  <verify>
    <automated>pytest tests/test_asl_validation.py -v --tb=short -k run_batch_replication</automated>
  </verify>
  <acceptance_criteria>
    - File is valid JSON with top-level `StartAt` + `States` (pytest passes for this file).
    - Contains `arn:aws:states:::aws-sdk:s3control:createJob`.
    - createJob Parameters include `"AccountId.$"`, `"ClientRequestToken.$": "States.UUID()"`, `"ConfirmationRequired": false`, `"Operation"` with key `"S3ReplicateObject"`, a `"Report"` object, and `"ManifestGenerator"` with `"S3JobManifestGenerator"` containing a `"Filter"`.
    - The createJob Task carries `"Credentials": {"RoleArn.$": "$.SourceAccount.RoleArn"}`.
    - A Choice branches between report-enabled and report-disabled paths based on `$.ReportBucketArn`.
    - No `arn:aws:states:::lambda:invoke`.
  </acceptance_criteria>
  <done>run_batch ASL dispatches a single S3ReplicateObject batch job with on-demand generated manifest, fresh idempotency token, optional report, returning JobId.</done>
</task>

<task type="auto">
  <name>Task 2: check_batch_replication.asl.json (describeJob poll loop)</name>
  <read_first>
    - modules/step-functions/efs/delete_replication.asl.json (Wait+Task+Choice poll-loop shell: WaitReplicationDeleted lines 103-107, CheckReplicationDeleted Task lines 108-132, IsReplicationDeleted Choice routing back to Wait lines 133-149 — copy the loop structure, swap to describeJob)
    - modules/step-functions/s3/run_batch_replication.asl.json (the file from Task 1 — consumes $.BatchJob.JobId it produces)
    - .planning/phases/07-s3-replication-module/07-RESEARCH.md (Pattern 3 lines 264-288: full describeJob shape + Job.Status enum + terminal states; D-08)
    - .planning/phases/07-s3-replication-module/07-PATTERNS.md (check_batch section lines 241-263)
  </read_first>
  <action>
    Create the file with "StartAt": "WaitForJob". State flow:
    (1) `WaitForJob` Wait, `Seconds: 30` (polling interval, D-11 discretion), Next `DescribeBatchJob`.
    (2) `DescribeBatchJob` Task -> `arn:aws:states:::aws-sdk:s3control:describeJob`, Parameters `{"AccountId.$":"$.SourceAccount.AccountId","JobId.$":"$.BatchJob.JobId"}`, `Credentials.RoleArn.$ = $.SourceAccount.RoleArn`, ResultPath `$.JobStatus`, Next `EvaluateJobStatus`, Catch `States.ALL` -> `BatchJobFailed`.
    (3) `EvaluateJobStatus` Choice on `$.JobStatus.Job.Status`: StringEquals "Complete" -> `BatchSucceeded`; StringEquals "Failed" -> `BatchJobFailed`; StringEquals "Cancelled" -> `BatchJobFailed`; Default -> `WaitForJob` (loop; covers New/Preparing/Ready/Active/Pausing/Paused/Cancelling/Failing).
    (4) `BatchSucceeded` Pass (Status "Completed", ModuleName "check_batch_replication", pass through `$.JobStatus.Job.ProgressSummary` if present) -> `CheckSucceeded` Succeed.
    (5) `BatchJobFailed` Fail, Error "BatchReplicationJobNotComplete", Cause referencing terminal Failed/Cancelled status. No lambda:invoke.
  </action>
  <verify>
    <automated>pytest tests/test_asl_validation.py -v --tb=short -k check_batch_replication</automated>
  </verify>
  <acceptance_criteria>
    - File is valid JSON with top-level `StartAt` + `States` (pytest passes for this file).
    - Contains `arn:aws:states:::aws-sdk:s3control:describeJob`.
    - Contains a `Wait` state and a `Choice` on `$.JobStatus.Job.Status` with explicit branches for `Complete`, `Failed`, and `Cancelled`, and a `Default` that routes back to the Wait state.
    - The describeJob Task carries `"Credentials": {"RoleArn.$": "$.SourceAccount.RoleArn"}` and passes `"AccountId.$"` explicitly.
    - Consumes `$.BatchJob.JobId` (the field produced by run_batch Task 1).
    - No `arn:aws:states:::lambda:invoke`.
  </acceptance_criteria>
  <done>check_batch ASL polls describeJob on a Wait loop, succeeding on Complete and failing on Failed/Cancelled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SFN execution -> source account | SFN assumes `$.SourceAccount.RoleArn` to call s3control createJob/describeJob |
| Batch job -> s3_replication role | The created job (not the SFN) assumes the batch replication role to perform copies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | replay of CreateJob | mitigate | Fresh `ClientRequestToken` via `States.UUID()` per execution (idempotency token; reuse throws IdempotencyException) |
| T-07-05 | Information Disclosure | batch completion report | accept | Report disabled by default when no `ReportBucketArn` provided (O1); when provided, report bucket grants are client-owned (out of scope) |
| T-07-06 | Elevation of Privilege | createJob RoleArn passing | mitigate | RoleArn comes from `$.BatchReplicationRoleArn` input; the `iam:PassRole` constraint enforcing this is in Plan 03 (source-account `iam:PassedToService`) |
| T-07-SC | Tampering | npm/pip/cargo installs | n/a | No package installs — Terraform + ASL JSON only |
</threat_model>

<verification>
- `pytest tests/test_asl_validation.py -v --tb=short` green for both new files.
- Structural read confirms no `lambda:invoke`, AccountId passed explicitly, States.UUID() token present.
</verification>

<success_criteria>
- Both `run_batch_replication.asl.json` and `check_batch_replication.asl.json` exist, are valid ASL, and pass structural validation.
- REPL-02 (createJob S3ReplicateObject + GeneratedManifest backfill), REPL-03 (describeJob polling to completion), REPL-05 (single job covers all destinations), and REPL-06 (native SDK only, no Lambda) are realized.
</success_criteria>

<output>
Create `.planning/phases/07-s3-replication-module/07-02-SUMMARY.md` when done.
</output>
