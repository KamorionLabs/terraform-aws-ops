---
phase: 07-s3-replication-module
plan: 04
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - modules/step-functions/s3/versions.tf
  - modules/step-functions/s3/variables.tf
  - modules/step-functions/s3/main.tf
  - modules/step-functions/s3/outputs.tf
autonomous: true
requirements: [REPL-01, REPL-02, REPL-03, REPL-04, REPL-05, REPL-06]
must_haves:
  truths:
    - "the s3 module declares all 4 SFN via a file()-based for_each (no templatefile, no Lambda)"
    - "the module exposes step_function_arns / step_function_names / log_group_arn outputs"
    - "the module follows the existing step-functions naming_convention (pascal/kebab) with Module=s3 tag"
    - "terraform validate passes for modules/step-functions/s3"
    - "the module contains NO archive_file, aws_lambda_function, or lambda IAM/log resources"
  artifacts:
    - path: "modules/step-functions/s3/main.tf"
      provides: "for_each aws_sfn_state_machine.s3 over the 4 ASL files + log group"
      contains: "aws_sfn_state_machine"
    - path: "modules/step-functions/s3/versions.tf"
      provides: "terraform/provider constraints (>= 1.0 / aws >= 5.0)"
      contains: "required_providers"
    - path: "modules/step-functions/s3/variables.tf"
      provides: "prefix, orchestrator_role_arn, logging/xray/naming variables"
      contains: "orchestrator_role_arn"
    - path: "modules/step-functions/s3/outputs.tf"
      provides: "SFN ARN/name maps + log group ARN"
      contains: "step_function_arns"
  key_links:
    - from: "main.tf aws_sfn_state_machine.s3"
      to: "the 4 *.asl.json files"
      via: "file(${path.module}/${each.value})"
      pattern: "file\\(.*each.value"
    - from: "main.tf"
      to: "var.orchestrator_role_arn"
      via: "role_arn assignment on every state machine"
      pattern: "role_arn = var.orchestrator_role_arn"
---

<objective>
Build the Terraform skeleton of `modules/step-functions/s3/` (versions/variables/main/outputs), wiring the 4 ASL state machines authored in Plans 01-02 into `aws_sfn_state_machine` resources via a plain `file()` for_each — lighter than `sync/` because there is NO Lambda (D-09). Make `terraform validate` pass for the module (REPL-01..06, Terraform side).

Purpose: A deployable module needs its `.tf` wrapper. The EFS module's `file()`-map + for_each + log group is the direct analog; drop EFS's templatefile/sub-SFN maps and moved blocks (s3/ has no sub-SFN ARN injection) and never copy `sync/`'s Lambda packaging.
Output: 4 `.tf` files; `terraform validate` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-s3-replication-module/07-CONTEXT.md
@.planning/phases/07-s3-replication-module/07-RESEARCH.md
@.planning/phases/07-s3-replication-module/07-PATTERNS.md
@.planning/phases/07-s3-replication-module/07-01-SUMMARY.md
@.planning/phases/07-s3-replication-module/07-02-SUMMARY.md

<interfaces>
<!-- EFS analog files to mirror (read them; do not invent structure). -->
modules/step-functions/efs/versions.tf   (copy verbatim — >= 1.0 / hashicorp/aws >= 5.0)
modules/step-functions/efs/variables.tf  (7 vars, copy verbatim: prefix, tags, orchestrator_role_arn, enable_logging=true, log_retention_days=30, enable_xray_tracing=false, naming_convention=pascal validated contains["pascal","kebab"])
modules/step-functions/efs/main.tf       (use ONLY locals.step_functions file()-map lines 6-25/40-47, the single aws_sfn_state_machine.efs block lines 53-75, and the log group lines 160-167 — DROP step_functions_sub/step_functions_templated maps, both templatefile resources, and all moved blocks)
modules/step-functions/efs/outputs.tf    (collapse the 3-way merge to a single map over aws_sfn_state_machine.s3)

<!-- The 4 ASL filenames the file()-map keys must reference (created in Plans 01-02): -->
setup_cross_account_replication.asl.json   (Plan 01)
delete_replication.asl.json                (Plan 01)
run_batch_replication.asl.json             (Plan 02)
check_batch_replication.asl.json           (Plan 02)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: versions.tf + variables.tf (copy EFS verbatim, S3-flavored docs)</name>
  <read_first>
    - modules/step-functions/efs/versions.tf (full — copy verbatim)
    - modules/step-functions/efs/variables.tf (full — copy verbatim, adjust only naming_convention doc examples to S3)
    - .planning/phases/07-s3-replication-module/07-PATTERNS.md (versions.tf lines 30-44, variables.tf lines 47-53 — explicit warning NOT to copy sync/variables.tf Lambda vars)
  </read_first>
  <action>
    Create `modules/step-functions/s3/versions.tf` byte-identical to the EFS one (`required_version >= 1.0`, `hashicorp/aws >= 5.0`). Create `modules/step-functions/s3/variables.tf` with the same 7 variables as EFS (`prefix`, `tags`, `orchestrator_role_arn`, `enable_logging` default true, `log_retention_days` default 30, `enable_xray_tracing` default false, `naming_convention` default "pascal" with the `contains(["pascal","kebab"])` validation). Change only the `naming_convention` description examples from EFS to S3 (e.g. "S3-SetupCrossAccountReplication" / "s3-setup-cross-account-replication"). Do NOT add any Lambda variables (`lambda_role_arn`, `cross_account_role_arns`, `log_level`) — D-09.
  </action>
  <verify>
    <automated>terraform -chdir=modules/step-functions/s3 init -backend=false && terraform -chdir=modules/step-functions/s3 fmt -check</automated>
  </verify>
  <acceptance_criteria>
    - versions.tf contains `required_providers` with `hashicorp/aws` `>= 5.0` and `required_version = ">= 1.0"`.
    - variables.tf declares exactly `prefix`, `tags`, `orchestrator_role_arn`, `enable_logging`, `log_retention_days`, `enable_xray_tracing`, `naming_convention` and NO Lambda-related variable.
    - `naming_convention` has the `contains(["pascal","kebab"])` validation block.
    - `terraform fmt -check` is clean for both files.
  </acceptance_criteria>
  <done>versions.tf and variables.tf present, Lambda-free, fmt-clean.</done>
</task>

<task type="auto">
  <name>Task 2: main.tf + outputs.tf and validate the module</name>
  <read_first>
    - modules/step-functions/efs/main.tf (locals.step_functions lines 6-25 + sfn_names lines 40-47, aws_sfn_state_machine.efs block lines 53-75, log group lines 160-167 — IGNORE step_functions_sub/templated maps lines 27-36, templatefile resources lines 81-140, moved blocks lines 146-154)
    - modules/step-functions/efs/outputs.tf (full — collapse the 3-way merge)
    - .planning/phases/07-s3-replication-module/07-PATTERNS.md (main.tf lines 55-113, outputs.tf lines 117-133)
    - .planning/phases/07-s3-replication-module/07-RESEARCH.md (Code Examples Terraform skeleton lines 378-410 — verified locals + resource block)
  </read_first>
  <action>
    Create `modules/step-functions/s3/main.tf`:
    - `locals.step_functions` map with exactly 4 keys -> filenames: `setup_cross_account_replication`, `run_batch_replication`, `check_batch_replication`, `delete_replication`.
    - `locals.sfn_names` for_each map applying naming_convention: pascal -> `"${var.prefix}-S3-${replace(title(replace(k,"_"," "))," ","")}"`, kebab -> `"${var.prefix}-s3-${replace(k,"_","-")}"`.
    - `resource "aws_sfn_state_machine" "s3"` with `for_each = local.step_functions`, `name = local.sfn_names[each.key]`, `role_arn = var.orchestrator_role_arn`, `definition = file("${path.module}/${each.value}")`, the same `logging_configuration` (enable_logging-gated, referencing `aws_cloudwatch_log_group.sfn[0].arn`), `tracing_configuration { enabled = var.enable_xray_tracing }`, and `tags = merge(var.tags, {Module = "s3", Name = local.sfn_names[each.key]})`.
    - `resource "aws_cloudwatch_log_group" "sfn"` `count = var.enable_logging ? 1 : 0`, name `"/aws/stepfunctions/${var.prefix}-s3"`, `retention_in_days = var.log_retention_days`, tags `var.tags`.
    - Do NOT add templatefile resources, sub-SFN maps, moved blocks, or any Lambda/archive_file/aws_lambda_function/aws_lambda_permission resources.
    Create `modules/step-functions/s3/outputs.tf` with `step_function_arns` = `{for k,v in aws_sfn_state_machine.s3 : k => v.arn}`, `step_function_names` = `{for k,v in aws_sfn_state_machine.s3 : k => v.name}`, and `log_group_arn` = `var.enable_logging ? aws_cloudwatch_log_group.sfn[0].arn : null` (single-map, not the EFS 3-way merge).
    Then run `terraform validate` for the module (the 4 ASL files from Plans 01-02 must exist for `file()` to resolve at validate time).
  </action>
  <verify>
    <automated>terraform -chdir=modules/step-functions/s3 init -backend=false && terraform -chdir=modules/step-functions/s3 validate && terraform -chdir=modules/step-functions/s3 fmt -check</automated>
  </verify>
  <acceptance_criteria>
    - main.tf contains `resource "aws_sfn_state_machine" "s3"` with `for_each = local.step_functions` and `definition = file("${path.module}/${each.value}")`.
    - `locals.step_functions` has exactly the 4 expected keys pointing at the 4 `.asl.json` filenames.
    - main.tf contains `aws_cloudwatch_log_group.sfn` and tags every machine with `Module = "s3"`.
    - main.tf contains NO `templatefile(`, NO `moved {`, NO `archive_file`, NO `aws_lambda_function`, NO `aws_lambda_permission`.
    - outputs.tf exposes `step_function_arns`, `step_function_names`, `log_group_arn` over the single `aws_sfn_state_machine.s3` resource.
    - `terraform -chdir=modules/step-functions/s3 validate` passes and `terraform fmt -check` is clean.
  </acceptance_criteria>
  <done>The s3 module wires all 4 ASL files into Step Functions resources with logging, exports the standard outputs, and validates Lambda-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Terraform module -> AWS account | Module declares Step Function resources executed under `var.orchestrator_role_arn` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Tampering | introducing compute surface via Lambda | mitigate | Module copies the EFS `file()`-only skeleton, never `sync/`'s Lambda packaging; acceptance criteria assert no `aws_lambda_function`/`archive_file` (D-09) |
| T-07-11 | Repudiation | missing execution audit | mitigate | `logging_configuration` enabled by default (enable_logging=true) routes execution data to a dedicated CloudWatch log group |
| T-07-SC | Tampering | npm/pip/cargo installs | n/a | No package installs — Terraform HCL only; provider pinned `hashicorp/aws >= 5.0` |
</threat_model>

<verification>
- `terraform -chdir=modules/step-functions/s3 init -backend=false && terraform validate` passes (requires Plans 01-02 ASL files present).
- `terraform fmt -check` clean across the 4 new .tf files.
- grep confirms no `aws_lambda_function`, `archive_file`, `templatefile(`, or `moved {` in main.tf.
- `pytest tests/test_asl_validation.py -v` still green (the 4 ASL files auto-discovered).
</verification>

<success_criteria>
- The `modules/step-functions/s3/` module deploys all 4 SFN from the ASL files, exports ARN/name maps, validates cleanly, and contains no Lambda.
- REPL-01..06 are realized on the Terraform side (the module renders and validates the 4 native-SDK state machines).
</success_criteria>

<output>
Create `.planning/phases/07-s3-replication-module/07-04-SUMMARY.md` when done.
</output>
