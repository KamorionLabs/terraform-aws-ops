---
phase: 03-consolidation
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - modules/step-functions/utils/run_archive_job.asl.json
  - modules/step-functions/utils/main.tf
  - modules/step-functions/utils/variables.tf
autonomous: true
requirements: [CON-04]

must_haves:
  truths:
    - "run_archive_job.asl.json contains both eks:runJob.sync and lambda:invoke job lifecycle, routed by CheckAccessModeForJob Choice on $.EKS.AccessMode"
    - "run_archive_job_private.asl.json is deleted"
    - "utils/main.tf no longer uses _eks_suffix variable interpolation"
    - "utils/variables.tf no longer contains eks_access_mode variable"
    - "No module in step-functions/ references eks_access_mode variable anymore"
  artifacts:
    - path: "modules/step-functions/utils/run_archive_job.asl.json"
      provides: "Consolidated run_archive_job with shared prep + dual job lifecycle"
      contains: "CheckAccessModeForJob"
    - path: "modules/step-functions/utils/main.tf"
      provides: "Utils module without _eks_suffix logic"
    - path: "modules/step-functions/utils/variables.tf"
      provides: "Utils variables without eks_access_mode"
  key_links:
    - from: "modules/step-functions/utils/main.tf"
      to: "modules/step-functions/utils/run_archive_job.asl.json"
      via: "file() in local.step_functions map"
      pattern: 'run_archive_job.*=.*"run_archive_job.asl.json"'
    - from: "modules/step-functions/utils/run_archive_job.asl.json"
      to: "$.EKS.AccessMode"
      via: "CheckAccessModeForJob Choice state"
      pattern: "EKS.AccessMode"
---

<objective>
Consolidate the last ASL pair (run_archive_job pub/priv) in the Utils module, then clean up: remove _eks_suffix from utils/main.tf, remove eks_access_mode from utils/variables.tf. After this plan, no Terraform module references eks_access_mode and all 6 _private files are eliminated.

Purpose: Completes the Phase 3 consolidation by handling the last complex pair (run_archive_job) and removing all traces of the compile-time eks_access_mode branching across all modules.

Output: 1 consolidated ASL file, updated utils/main.tf and utils/variables.tf, 1 deleted _private file, zero eks_access_mode references anywhere.
</objective>

<execution_context>
@/Users/clark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/clark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-consolidation/03-CONTEXT.md
@.planning/phases/03-consolidation/03-RESEARCH.md
@.planning/phases/03-consolidation/03-01-SUMMARY.md
@.planning/phases/03-consolidation/03-02-SUMMARY.md

<interfaces>
<!-- Utils module Terraform structure -->
From modules/step-functions/utils/main.tf:
```hcl
locals {
  _eks_suffix = var.eks_access_mode == "private" ? "_private" : ""
  step_functions = {
    tag_resources           = "tag_resources.asl.json"
    run_archive_job         = "run_archive_job${local._eks_suffix}.asl.json"
    prepare_refresh         = "prepare_refresh.asl.json"
    cleanup_and_stop        = "cleanup_and_stop.asl.json"
    notify                  = "notify.asl.json"
    validate_refresh_config = "validate_refresh_config.asl.json"
  }
}
```

From 03-RESEARCH.md -- run_archive_job pattern:
- Shared prep: GetClusterInfo, EnsureNodegroupCapacity, GetDatabaseCredentials, ParseSecretString (4 states)
- Public: GetEksClusterInfo + RunKubernetesJob (eks:runJob.sync) = 2 states
- Private: CreateArchiveJob + WaitForArchiveJob + CheckArchiveJobStatus + ArchiveJobStatusChoice + DeleteArchiveJob + DeleteArchiveJobOnError + ArchiveJobFailed = 7 states
- Shared terminal: PrepareOutput, ArchiveFailed, ArchiveSucceeded (3 states)
- Estimated consolidated: ~17 states total
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate run_archive_job ASL pair into unified file</name>
  <files>
    modules/step-functions/utils/run_archive_job.asl.json
    modules/step-functions/utils/run_archive_job_private.asl.json (delete)
  </files>
  <read_first>
    modules/step-functions/utils/run_archive_job.asl.json
    modules/step-functions/utils/run_archive_job_private.asl.json
    .planning/phases/03-consolidation/03-RESEARCH.md
    .planning/phases/03-consolidation/03-01-SUMMARY.md
    .planning/phases/03-consolidation/03-02-SUMMARY.md
  </read_first>
  <action>
Create a consolidated run_archive_job.asl.json merging both pub and priv variants. This follows the complex consolidation pattern from 03-RESEARCH.md.

**Consolidated structure:**

1. Keep the shared preparation states from the public variant as the initial flow:
   - StartAt remains the first shared prep state (likely GetClusterInfo or similar)
   - GetClusterInfo -> EnsureNodegroupCapacity -> GetDatabaseCredentials -> ParseSecretString
   - These are identical between pub and priv

2. After ParseSecretString (last shared prep state), add `CheckAccessModeForJob` Choice state:
   - `$.EKS.AccessMode` StringEquals `"public"` -> `GetEksClusterInfo`
   - Default -> `CreateArchiveJob` (first private job state)

3. Public branch (2 states from public variant):
   - `GetEksClusterInfo` (eks:describeCluster, Credentials with DestinationAccount.RoleArn, ResultPath $.EksCluster) -> `RunKubernetesJob`
   - `RunKubernetesJob` (Resource: eks:runJob.sync, uses ClusterName/CertificateAuthority/Endpoint from $.EksCluster.Cluster, full job spec in Parameters) -> PrepareOutput
   - Keep all Retry, Catch blocks from the public original

4. Private branch (7 states from private variant):
   - `CreateArchiveJob` (lambda:invoke via K8sProxyLambdaArn, Payload with Action "createJob") -> `WaitForArchiveJob`
   - `WaitForArchiveJob` (Wait state) -> `CheckArchiveJobStatus`
   - `CheckArchiveJobStatus` (lambda:invoke, Action "getJobStatus") -> `ArchiveJobStatusChoice`
   - `ArchiveJobStatusChoice` (Choice): Active/Running -> WaitForArchiveJob (loop), Complete/Succeeded -> `DeleteArchiveJob`, Failed -> `DeleteArchiveJobOnError`
   - `DeleteArchiveJob` (lambda:invoke, Action "deleteJob") -> PrepareOutput
   - `DeleteArchiveJobOnError` (lambda:invoke, Action "deleteJob") -> `ArchiveJobFailed` (Fail)
   - `ArchiveJobFailed` (Fail state with error details)
   - Keep all Parameters, Credentials, ResultSelector, Retry, Catch from the private original

5. Shared terminal states: PrepareOutput (Pass), ArchiveFailed (Fail), ArchiveSucceeded (Succeed)

6. Update the Comment field to indicate dual-mode support: "Runs archive Kubernetes job. Supports both public (eks:runJob.sync) and private (k8s-proxy Lambda) access modes."

After creating the consolidated file, delete run_archive_job_private.asl.json.

Preserve every Retry, Catch, ResultSelector, Credentials block exactly from its source variant. The public eks:runJob.sync may have different error codes than the private lambda:invoke cycle.
  </action>
  <verify>
    <automated>cd /Users/clark/work/kamorion/terraform-aws-ops && python -m pytest tests/test_asl_validation.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - run_archive_job.asl.json contains "CheckAccessModeForJob" state
    - run_archive_job.asl.json contains "EKS.AccessMode" string
    - run_archive_job.asl.json contains "eks:runJob.sync" or "eks:runJob" (public path)
    - run_archive_job.asl.json contains "lambda:invoke" (private path)
    - run_archive_job.asl.json contains "GetEksClusterInfo" state (public-only)
    - run_archive_job.asl.json contains "CreateArchiveJob" state (private cycle)
    - run_archive_job.asl.json contains "WaitForArchiveJob" state (private cycle)
    - run_archive_job.asl.json contains "DeleteArchiveJob" state (private cleanup)
    - run_archive_job.asl.json contains "RunKubernetesJob" state (public path)
    - run_archive_job_private.asl.json does NOT exist
    - Consolidated file passes JSON syntax validation
    - pytest tests/test_asl_validation.py passes
  </acceptance_criteria>
  <done>Consolidated run_archive_job.asl.json with shared prep + dual job lifecycle paths, _private file deleted, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Update Utils Terraform module and final cleanup verification</name>
  <files>
    modules/step-functions/utils/main.tf
    modules/step-functions/utils/variables.tf
  </files>
  <read_first>
    modules/step-functions/utils/main.tf
    modules/step-functions/utils/variables.tf
    modules/step-functions/eks/variables.tf
    modules/step-functions/db/variables.tf
  </read_first>
  <action>
**main.tf changes:**
1. Remove the `_eks_suffix` local variable line: `_eks_suffix = var.eks_access_mode == "private" ? "_private" : ""`
2. Remove `${local._eks_suffix}` interpolation from the run_archive_job entry. It becomes:
```hcl
    run_archive_job = "run_archive_job.asl.json"
```
3. Keep everything else unchanged: all other step_functions entries, aws_sfn_state_machine.utils resource, logging, tags.

**variables.tf changes:**
1. Remove the entire `variable "eks_access_mode"` block.
2. Keep all other variables unchanged.

Run `terraform fmt` on both files after editing.

**Final cross-module verification (read-only checks, no modifications):**
After editing, verify that NO module under modules/step-functions/ still references eks_access_mode:
- Confirm eks/variables.tf has no eks_access_mode (done in 03-01)
- Confirm db/variables.tf has no eks_access_mode (done in 03-02)
- Confirm utils/variables.tf has no eks_access_mode (done in this task)
- Confirm no _private.asl.json files exist anywhere under modules/step-functions/

Note: The root module's main.tf and variables.tf do NOT need changes -- eks_access_mode was never passed to any submodule from root (all 3 modules use the default). The research confirmed this: "The root module never passes eks_access_mode."
  </action>
  <verify>
    <automated>cd /Users/clark/work/kamorion/terraform-aws-ops && terraform fmt -check modules/step-functions/utils/main.tf modules/step-functions/utils/variables.tf && echo "FMT OK" && grep -r "eks_access_mode" modules/step-functions/ && echo "FAIL: eks_access_mode still referenced" || echo "CLEAN: no eks_access_mode references" && find modules/step-functions/ -name "*_private.asl.json" | head -5 && echo "FAIL: _private files remain" || echo "CLEAN: no _private files" && python -m pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - modules/step-functions/utils/main.tf does NOT contain "_eks_suffix"
    - modules/step-functions/utils/main.tf does NOT contain "eks_access_mode"
    - modules/step-functions/utils/main.tf contains `run_archive_job = "run_archive_job.asl.json"` (direct filename)
    - modules/step-functions/utils/variables.tf does NOT contain "eks_access_mode"
    - terraform fmt -check passes on both files
    - grep -r "eks_access_mode" modules/step-functions/ returns no results (zero references across all 3 modules)
    - find modules/step-functions/ -name "*_private.asl.json" returns no results (zero _private files across all modules)
    - pytest tests/ full suite passes
  </acceptance_criteria>
  <done>Utils module cleaned, eks_access_mode variable eliminated from all modules, zero _private files remain, full test suite passes</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -x -q` passes (full suite)
- `test ! -f modules/step-functions/utils/run_archive_job_private.asl.json` returns 0
- `terraform fmt -check modules/step-functions/utils/` passes
- `grep -r "eks_access_mode" modules/step-functions/` returns nothing (exit code 1)
- `find modules/step-functions/ -name "*_private.asl.json" -type f` returns nothing
- `grep -c "EKS.AccessMode" modules/step-functions/utils/run_archive_job.asl.json` returns >= 1
- All 6 consolidated files contain "CheckAccessMode" or similar Choice state
</verification>

<success_criteria>
- Consolidated run_archive_job with dual job lifecycle paths
- _private file deleted
- Utils Terraform module uses direct filenames
- eks_access_mode variable removed from ALL 3 modules (EKS, DB, Utils)
- Zero _private.asl.json files remain anywhere
- All tests pass (full suite)
</success_criteria>

<output>
After completion, create `.planning/phases/03-consolidation/03-03-SUMMARY.md`
</output>
