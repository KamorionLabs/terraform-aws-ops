---
phase: 01-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_asl_validation.py
  - tests/test_stepfunctions_local.py
  - .github/workflows/step-functions.yml
autonomous: true
requirements: [PRE-01, TST-02]

must_haves:
  truths:
    - "strip_credentials supprime tous les blocs Credentials des definitions ASL avant enregistrement dans SFN Local"
    - "Le CI matrix EFS couvre tous les fichiers ASL existants du module EFS (pas juste delete_filesystem)"
    - "Les tests SFN Local utilisent strip_credentials via la fixture create_state_machine"
    - "L'audit $$.Execution.Input est formalise et les fichiers concernes sont documentes"
  artifacts:
    - path: "tests/conftest.py"
      provides: "strip_credentials helper + fixture create_state_machine mise a jour"
      contains: "strip_credentials"
    - path: "tests/test_asl_validation.py"
      provides: "Tests Comment et Catch auto-contenu pour les sous-SFN"
      contains: "TestASLComment"
    - path: "tests/test_stepfunctions_local.py"
      provides: "Tests SFN Local utilisant strip_credentials via fixture"
      contains: "strip_credentials"
    - path: ".github/workflows/step-functions.yml"
      provides: "Matrix EFS etendu avec tous les fichiers existants + 3 futurs sous-SFN"
      contains: "manage_lambda_lifecycle"
  key_links:
    - from: "tests/test_stepfunctions_local.py"
      to: "tests/conftest.py"
      via: "create_state_machine fixture"
      pattern: "create_state_machine"
    - from: ".github/workflows/step-functions.yml"
      to: "modules/step-functions/efs/*.asl.json"
      via: "matrix step_function entries"
      pattern: "validate-efs-module"
---

<objective>
Reparer le CI et preparer l'infrastructure de test pour les sous-SFN.

Purpose: Les tests SFN Local echouent sur les definitions cross-account (champs Credentials non supportes par le simulateur local). Le matrix CI EFS ne couvre qu'un seul fichier. Ces deux problemes bloquent la validation des sous-SFN creees dans les plans suivants. L'audit $$.Execution.Input doit aussi etre formalise comme pre-requis a l'extraction.

Output: CI vert pour tous les fichiers EFS existants, fixture create_state_machine qui strip les Credentials, tests prets pour les nouvelles sous-SFN (Comment, Catch nomme).
</objective>

<execution_context>
@/Users/clark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/clark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-extraction/01-CONTEXT.md
@.planning/phases/01-extraction/01-RESEARCH.md
@.planning/phases/01-extraction/01-VALIDATION.md

<interfaces>
<!-- Existing code contracts the executor needs -->

From tests/conftest.py:
```python
def get_asl_files() -> list[Path]:
    """Get all ASL files in the project."""

@pytest.fixture(scope="session")
def sfn_client():
    """Create Step Functions client connected to local endpoint."""

@pytest.fixture
def create_state_machine(sfn_client):
    """Factory fixture to create state machines for testing."""
    # Line 77: sfn_client.create_state_machine(name=name, definition=json.dumps(definition), ...)
    # NOTE: Currently sends raw definition WITHOUT stripping Credentials
```

From tests/test_asl_validation.py:
```python
ASL_FILES = get_asl_files()  # Auto-discovered via rglob("*.asl.json")
# Existing test classes: TestASLJsonSyntax, TestASLRequiredFields, TestASLStateTypes,
#   TestASLStateTransitions, TestASLTaskStates, TestASLChoiceStates, TestASLCrossAccount
```

From tests/test_stepfunctions_local.py:
```python
@pytest.mark.sfn_local
class TestStateMachineCreation:
    def test_create_state_machine(self, test_id, asl_file, sfn_client):
        # Line 51: sfn_client.create_state_machine(definition=json.dumps(definition), ...)
        # NOTE: Does NOT use create_state_machine fixture, calls sfn_client directly
        # This is the SECOND place where strip_credentials must be applied
```

From .github/workflows/step-functions.yml:
```yaml
validate-efs-module:
  strategy:
    matrix:
      step_function:
        - delete_filesystem   # ONLY entry - must be expanded to all 10+ EFS files
  # Line 198: exit 0 if file not found (permissive) - change to exit 1 for existing files
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter strip_credentials et mettre a jour les fixtures de test</name>
  <files>tests/conftest.py, tests/test_stepfunctions_local.py, tests/test_asl_validation.py</files>
  <action>
1. Dans `tests/conftest.py`, ajouter la fonction `strip_credentials(definition: dict) -> dict` AVANT les fixtures (apres les imports). Implementation :
   - `import copy` en haut du fichier
   - Faire un `copy.deepcopy(definition)`, iterer sur `clean.get("States", {}).items()`, appeler `state_def.pop("Credentials", None)` sur chaque state, retourner le dict nettoye
   - Gerer aussi les states imbriques dans Parallel/Map branches (iterer recursivement sur `Branches` pour Parallel et `Iterator`/`ItemProcessor` pour Map)

2. Dans `tests/conftest.py`, modifier la fixture `create_state_machine` (ligne 66-86) :
   - Ajouter l'appel `clean_definition = strip_credentials(definition)` juste avant `sfn_client.create_state_machine()`
   - Passer `json.dumps(clean_definition)` au lieu de `json.dumps(definition)` dans l'appel `create_state_machine`

3. Dans `tests/test_stepfunctions_local.py`, modifier `TestStateMachineCreation.test_create_state_machine` (ligne 42-70) :
   - Importer `strip_credentials` depuis conftest : ajouter `from conftest import strip_credentials` en haut du fichier (ou utiliser le import relatif `from .conftest import strip_credentials` — tester ce qui marche avec la structure pytest)
   - Alternative plus propre : changer la methode pour utiliser la fixture `create_state_machine` au lieu d'appeler `sfn_client.create_state_machine` directement. Remplacer le parametre `sfn_client` par `create_state_machine` dans la signature, et appeler `create_state_machine(sm_name, definition)`. Supprimer le bloc try/finally de cleanup (la fixture le gere).

4. Dans `tests/test_asl_validation.py`, ajouter deux nouvelles classes de test A LA FIN du fichier :

   `TestASLComment` : parametrise sur ASL_FILES, verifie que chaque fichier ASL a un champ `Comment` de type string non vide. Ce test couvre SUB-04 pour les futures sous-SFN (auto-decouvertes).

   `TestASLCatchSelfContained` : parametrise sur ASL_FILES, pour chaque fichier ASL qui contient au moins un state de type `Fail`, verifie que le state Fail a un champ `Error` non vide (nomme). Ce test couvre SUB-05 pour les futures sous-SFN. Ne pas rendre ce test trop strict — les SFN existantes n'ont pas toutes ce pattern. Filtrer : n'appliquer la verification que sur les fichiers dans `modules/step-functions/efs/manage_*.asl.json` (les sous-SFN). Utiliser un filtre sur le path pour ne cibler que les fichiers `manage_` du module EFS.
  </action>
  <verify>
    <automated>cd /Users/clark/work/kamorion/terraform-aws-ops && pytest tests/test_asl_validation.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>
    - strip_credentials existe dans conftest.py et est appelee dans create_state_machine fixture
    - test_stepfunctions_local.py utilise la fixture au lieu d'appeler sfn_client directement
    - TestASLComment et TestASLCatchSelfContained existent dans test_asl_validation.py
    - pytest tests/test_asl_validation.py passe (tests existants non casses)
  </done>
</task>

<task type="auto">
  <name>Task 2: Etendre le matrix CI EFS et formaliser l'audit $$.Execution.Input</name>
  <files>.github/workflows/step-functions.yml</files>
  <action>
1. Dans `.github/workflows/step-functions.yml`, modifier le job `validate-efs-module` (ligne 179-215) :
   - Remplacer le matrix actuel (une seule entree `delete_filesystem`) par la liste complete des fichiers EFS existants :
     ```yaml
     matrix:
       step_function:
         - check_replication_sync
         - cleanup_efs_lambdas
         - create_filesystem
         - delete_filesystem
         - delete_replication
         - get_subpath_and_store_in_ssm
         - restore_from_backup
         - setup_cross_account_replication
         # Phase 1 sub-SFNs (will be created in Plan 02/03)
         - manage_filesystem_policy
         - manage_access_point
         - manage_lambda_lifecycle
     ```
   - Changer le comportement quand le fichier n'existe pas : remplacer `exit 0` (ligne 199) par `exit 1` pour les fichiers existants. MAIS garder `exit 0` pour les 3 nouveaux fichiers `manage_*` qui n'existent pas encore. Solution : ajouter un commentaire et garder `exit 0` pour l'instant (les fichiers seront crees dans Plan 02/03, et a ce moment le `exit 0` n'aura plus d'effet car les fichiers existeront). Alternative : utiliser un check conditionnel : si le fichier est dans la liste des "known future" files, exit 0, sinon exit 1. La solution la plus simple : garder `exit 0` temporairement — c'est coherent avec le fait que les fichiers seront crees dans les plans suivants de cette meme phase.

2. Formaliser l'audit TST-02 : ajouter un commentaire dans le fichier workflow ou dans un step de validation qui documente les resultats de l'audit $$.Execution.Input. Alternative plus propre : l'audit est deja documente dans 01-RESEARCH.md (lignes 235-239). Pour TST-02, ajouter un step dans le job `validate-local` qui execute le scan automatiquement :
   ```yaml
   - name: Audit Execution.Input references in sub-SFNs
     run: |
       echo "Scanning for $$.Execution.Input in sub-SFN files..."
       # Sub-SFNs must NOT use $$.Execution.Input (scope loss risk)
       for f in modules/step-functions/efs/manage_*.asl.json; do
         if [ -f "$f" ] && grep -q 'Execution.Input' "$f"; then
           echo "FAIL: $f contains \$\$.Execution.Input reference (scope loss risk)"
           exit 1
         fi
       done
       echo "OK: No sub-SFN files contain \$\$.Execution.Input references"
   ```
   Ce step s'executera automatiquement quand les fichiers manage_*.asl.json existeront — protection continue contre la regression.
  </action>
  <verify>
    <automated>cd /Users/clark/work/kamorion/terraform-aws-ops && python -c "import yaml; y=yaml.safe_load(open('.github/workflows/step-functions.yml')); efs=y['jobs']['validate-efs-module']['strategy']['matrix']['step_function']; print(f'EFS matrix entries: {len(efs)}'); assert len(efs) >= 11, f'Expected >= 11, got {len(efs)}'; print('OK')" 2>&1 || echo "yaml not available, checking with grep" && grep -c 'manage_' .github/workflows/step-functions.yml</automated>
  </verify>
  <done>
    - Le matrix validate-efs-module contient >= 11 entrees (8 existants + 3 futurs sous-SFN)
    - Un step d'audit $$.Execution.Input est present dans le workflow pour scanner les fichiers manage_*.asl.json
    - Le workflow YAML est syntaxiquement valide
  </done>
</task>

</tasks>

<verification>
1. `pytest tests/test_asl_validation.py -v --tb=short` — tous les tests passent (existants + nouveaux Comment/Catch)
2. `python -c "import yaml; yaml.safe_load(open('.github/workflows/step-functions.yml'))"` — YAML valide
3. `grep -c 'strip_credentials' tests/conftest.py` — au moins 2 occurrences (definition + appel)
4. `grep -c 'manage_' .github/workflows/step-functions.yml` — au moins 3 occurrences
</verification>

<success_criteria>
- strip_credentials est integre dans le chemin de test (conftest fixture + test_stepfunctions_local)
- Le matrix CI EFS est complet (8 fichiers existants + 3 futurs)
- Les classes TestASLComment et TestASLCatchSelfContained sont pretes a valider les futures sous-SFN
- L'audit $$.Execution.Input est automatise dans le CI
- Aucun test existant n'est casse
</success_criteria>

<output>
After completion, create `.planning/phases/01-extraction/01-01-SUMMARY.md`
</output>
