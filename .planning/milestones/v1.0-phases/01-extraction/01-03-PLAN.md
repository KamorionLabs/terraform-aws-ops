---
phase: 01-extraction
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - modules/step-functions/efs/manage_lambda_lifecycle.asl.json
  - modules/step-functions/efs/manage_access_point.asl.json
  - modules/step-functions/efs/README.md
autonomous: true
requirements: [SUB-01, SUB-02, SUB-04, SUB-05, TST-01]

must_haves:
  truths:
    - "ManageLambdaLifecycle sous-SFN existe avec ~8 states gerant le lifecycle complet (check/create/wait/invoke)"
    - "ManageAccessPoint sous-SFN existe avec ~4 states gerant la creation/suppression d'access points"
    - "Les deux sous-SFN gerent leurs erreurs via des states Fail nommes propres"
    - "Aucune des sous-SFN ne contient de reference $$.Execution.Input"
    - "Les contrats I/O sont documentes dans Comment ASL et README.md"
    - "Tous les tests ASL passent pour les 3 sous-SFN (auto-decouverte rglob)"
  artifacts:
    - path: "modules/step-functions/efs/manage_lambda_lifecycle.asl.json"
      provides: "Sous-SFN ManageLambdaLifecycle extraite de check_replication_sync et get_subpath_and_store_in_ssm"
      contains: "ManageLambdaLifecycleFailed"
    - path: "modules/step-functions/efs/manage_access_point.asl.json"
      provides: "Sous-SFN ManageAccessPoint extraite de check_replication_sync et get_subpath_and_store_in_ssm"
      contains: "ManageAccessPointFailed"
    - path: "modules/step-functions/efs/README.md"
      provides: "Documentation I/O complete pour les 3 sous-SFN"
      contains: "ManageLambdaLifecycle"
  key_links:
    - from: "modules/step-functions/efs/main.tf"
      to: "modules/step-functions/efs/manage_lambda_lifecycle.asl.json"
      via: "local.step_functions map entry (cle ajoutee dans Plan 02)"
      pattern: "manage_lambda_lifecycle"
    - from: "modules/step-functions/efs/main.tf"
      to: "modules/step-functions/efs/manage_access_point.asl.json"
      via: "local.step_functions map entry (cle ajoutee dans Plan 02)"
      pattern: "manage_access_point"
---

<objective>
Creer les deux sous-SFN restantes (ManageLambdaLifecycle, ManageAccessPoint) en remplacement des stubs.

Purpose: ManageLambdaLifecycle est la plus complexe (~8 states) et la plus impactante (elimine ~24 states dupliques). ManageAccessPoint est plus simple (~4 states, ~12 states dupliques elimines). Les deux suivent le pattern valide par ManageFileSystemPolicy dans Plan 02. La resolution des references $$.Execution.Input dans check_replication_sync est critique pour ManageLambdaLifecycle.

Output: Les 3 sous-SFN sont completes, README documente, tous les tests passent.
</objective>

<execution_context>
@/Users/clark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/clark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-extraction/01-CONTEXT.md
@.planning/phases/01-extraction/01-RESEARCH.md
@.planning/phases/01-extraction/01-02-SUMMARY.md

<interfaces>
<!-- Pattern valide dans Plan 02 — suivre le meme format -->

From modules/step-functions/efs/manage_filesystem_policy.asl.json (reference pattern):
- Comment field with "ManageFileSystemPolicy | Input: {...} | Output: {...}"
- Catch blocks on all Task states with ErrorEquals: ["States.ALL"], ResultPath: "$.Error", Next: "HandleError"
- HandleError state: Type: Fail, Error: "ManageFileSystemPolicyFailed"
- No $$.Execution.Input references

From modules/step-functions/efs/main.tf:
```hcl
# Les cles existent deja (ajoutees dans Plan 02)
manage_lambda_lifecycle  = "manage_lambda_lifecycle.asl.json"
manage_access_point      = "manage_access_point.asl.json"
```

<!-- Source blocks to extract from (Lambda Lifecycle pattern) -->

From modules/step-functions/efs/check_replication_sync.asl.json:
- Pattern Lambda lifecycle: EnsureLambdaExists (getFunction) -> si ResourceNotFoundException -> CreateLambda (createFunction) -> WaitLambdaReady -> InvokeLambda
- ATTENTION: check_replication_sync.asl.json utilise $$.Execution.Input aux lignes 31, 50-51 pour SourceSubpathSSMParameter et DestinationSubpathSSMParameter
- Ces references sont dans les states SetSubpathDefaults et ApplySubpathSSMParams — PAS dans les blocs Lambda lifecycle directement
- Les blocs Lambda lifecycle eux-memes sont propres (pas de $$.Execution.Input)
- Les 2 blocs Lambda lifecycle sont pour source et destination (2 Lambdas differentes)

From modules/step-functions/efs/get_subpath_and_store_in_ssm.asl.json:
- Meme pattern Lambda lifecycle: EnsureLambdaExists -> CreateLambda -> WaitLambdaReady -> invoke
- PROPRE: 0 reference $$.Execution.Input (confirme par audit)
- Pattern plus complet: inclut aussi CreateAccessPoint et InvokeLambda post-creation

<!-- Source blocks to extract from (Access Point pattern) -->

From modules/step-functions/efs/get_subpath_and_store_in_ssm.asl.json:
- Pattern Access Point: CreateAccessPoint (aws-sdk:efs:createAccessPoint) -> attache a la Lambda via VPC mount
- ~4 states: Check/Create/Attach/Verify

From modules/step-functions/efs/check_replication_sync.asl.json:
- Pattern Access Point: CreateAccessPoint -> utilise dans le contexte de check flag file
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Creer manage_access_point.asl.json (remplacer le stub)</name>
  <files>modules/step-functions/efs/manage_access_point.asl.json</files>
  <action>
Remplacer le stub de `modules/step-functions/efs/manage_access_point.asl.json` par la sous-SFN complete.

**Analyse des sources avant ecriture :**
1. Lire `get_subpath_and_store_in_ssm.asl.json` en entier — identifier les states qui gerent les access points EFS (pattern createAccessPoint / deleteAccessPoint / describeAccessPoints)
2. Lire `check_replication_sync.asl.json` en entier — identifier le pattern access point utilise dans le contexte de check flag file
3. Identifier le contrat Input commun : FileSystemId, AccessPointConfig (PosixUser, RootDirectory, Tags), Account (RoleArn)
4. Confirmer qu'aucun des states ne reference `$$.Execution.Input`

**Structure de la sous-SFN (~4 states) :**
- Champ `Comment` obligatoire avec format: `"ManageAccessPoint | Input: {FileSystemId, AccessPointConfig, Account} | Output: {AccessPointId, AccessPointArn, Status}"`
- States logiques attendus (a adapter selon le code source exact) :
  1. CreateAccessPoint — aws-sdk:efs:createAccessPoint avec Credentials cross-account
  2. WaitAccessPointAvailable (optionnel) — Wait ou Check status
  3. State de succes (End: true) retournant AccessPointId et AccessPointArn
  4. HandleError — state `Fail` avec `Error: "ManageAccessPointFailed"`

**Regles identiques a ManageFileSystemPolicy :**
- Catch auto-contenu (SUB-05) sur chaque Task state
- Comment avec contrat I/O (SUB-04)
- Pas de $$.Execution.Input
- Suivre le meme QueryLanguage que les sources (JSONPath sauf si source utilise JSONata)
  </action>
  <verify>
    <automated>cd /Users/clark/work/kamorion/terraform-aws-ops && python -c "
import json
with open('modules/step-functions/efs/manage_access_point.asl.json') as f:
    d = json.load(f)
assert 'Comment' in d and 'ManageAccessPoint' in d['Comment'], 'Missing Comment'
assert d['StartAt'] in d['States'], 'StartAt refs missing state'
assert d['States'].get('Placeholder') is None, 'Still a stub!'
fail_states = [k for k,v in d['States'].items() if v.get('Type') == 'Fail']
assert any('ManageAccessPointFailed' in d['States'][s].get('Error','') for s in fail_states), 'Fail not named'
content = json.dumps(d)
assert 'Execution.Input' not in content, 'Contains $$.Execution.Input!'
print(f'OK: {len(d[\"States\"])} states, no scope loss')
"</automated>
  </verify>
  <done>
    - manage_access_point.asl.json est une sous-SFN complete (plus un stub)
    - ~4 states avec Catch auto-contenu
    - State Fail nomme ManageAccessPointFailed
    - Aucune reference $$.Execution.Input
    - JSON syntaxiquement valide
  </done>
</task>

<task type="auto">
  <name>Task 2: Creer manage_lambda_lifecycle.asl.json et finaliser README.md</name>
  <files>modules/step-functions/efs/manage_lambda_lifecycle.asl.json, modules/step-functions/efs/README.md</files>
  <action>
**Partie 1 : Creer manage_lambda_lifecycle.asl.json**

Remplacer le stub par la sous-SFN complete.

**Analyse des sources avant ecriture :**
1. Lire `check_replication_sync.asl.json` en entier — identifier les blocs de Lambda lifecycle (pattern EnsureLambdaExists/CreateLambda/WaitLambdaReady/InvokeLambda). Il y a 2 instances (source Lambda et destination Lambda).
2. Lire `get_subpath_and_store_in_ssm.asl.json` en entier — identifier le meme pattern (EnsureLambdaExists -> CreateLambda -> WaitLambdaReady)
3. Extraire le pattern commun generalise

**ATTENTION CRITIQUE — References $$.Execution.Input :**
Les references $$.Execution.Input dans check_replication_sync (lignes 31, 50-51) concernent `SourceSubpathSSMParameter` et `DestinationSubpathSSMParameter`. Ces references sont dans les states `SetSubpathDefaults` et `ApplySubpathSSMParams` qui sont HORS du bloc Lambda lifecycle. Les blocs Lambda lifecycle eux-memes sont propres.
Cependant, verifier systematiquement avant d'ecrire : `grep -n 'Execution.Input' check_replication_sync.asl.json` et s'assurer qu'aucune ref n'est dans les states extraits.

**Structure de la sous-SFN (~8 states) :**
- Champ `Comment` : `"ManageLambdaLifecycle | Input: {LambdaConfig, Account} | Output: {LambdaArn, LambdaName, Status}"`
- States logiques attendus :
  1. CheckLambdaExists — aws-sdk:lambda:getFunction, Catch ResourceNotFoundException
  2. CreateLambda — aws-sdk:lambda:createFunction (avec tous les params: Runtime, Handler, Role, Code, VpcConfig, etc.)
  3. WaitLambdaReady — Wait (quelques secondes) ou loop de check getFunction.State == Active
  4. VerifyLambdaActive — Check que la Lambda est prete
  5. LambdaReady — state de succes (End: true) retournant LambdaArn et LambdaName
  6. HandleError — state Fail avec Error: "ManageLambdaLifecycleFailed"

  Les states exacts dependent du code source — adapter la structure en fonction.

**Memes regles que les 2 sous-SFN precedentes : Catch auto-contenu, Comment I/O, pas de $$.Execution.Input.**

**Partie 2 : Finaliser README.md**

Mettre a jour `modules/step-functions/efs/README.md` :
- Remplacer les placeholders ManageLambdaLifecycle et ManageAccessPoint par la documentation complete
- Pour chaque sous-SFN documenter : schema Input (JSON), schema Output (JSON), Catch behavior (Error name), nombre de states approximatif
- Garder la section Integration Pattern (templatefile) deja creee dans Plan 02
  </action>
  <verify>
    <automated>cd /Users/clark/work/kamorion/terraform-aws-ops && python -c "
import json
# Validate manage_lambda_lifecycle
with open('modules/step-functions/efs/manage_lambda_lifecycle.asl.json') as f:
    d = json.load(f)
assert 'Comment' in d and 'ManageLambdaLifecycle' in d['Comment'], 'Missing Comment'
assert d['StartAt'] in d['States'], 'StartAt refs missing state'
assert d['States'].get('Placeholder') is None, 'Still a stub!'
fail_states = [k for k,v in d['States'].items() if v.get('Type') == 'Fail']
assert any('ManageLambdaLifecycleFailed' in d['States'][s].get('Error','') for s in fail_states), 'Fail not named'
content = json.dumps(d)
assert 'Execution.Input' not in content, 'Contains scope loss ref!'
print(f'ManageLambdaLifecycle OK: {len(d[\"States\"])} states')

# Validate manage_access_point (from Task 1)
with open('modules/step-functions/efs/manage_access_point.asl.json') as f:
    d2 = json.load(f)
assert d2['States'].get('Placeholder') is None, 'AccessPoint still stub!'

# Check README has all 3 documented
with open('modules/step-functions/efs/README.md') as f:
    readme = f.read()
for name in ['ManageFileSystemPolicy', 'ManageLambdaLifecycle', 'ManageAccessPoint']:
    assert name in readme, f'README missing {name}'
assert 'templatefile' in readme, 'README missing templatefile pattern'
print('README OK: all 3 sub-SFNs documented')
" && pytest tests/test_asl_validation.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>
    - manage_lambda_lifecycle.asl.json est une sous-SFN complete avec ~8 states
    - Aucun stub restant (les 3 fichiers ASL sont complets)
    - README.md documente les contrats I/O des 3 sous-SFN + pattern templatefile
    - Tous les tests ASL passent (auto-decouverte couvre les 3 fichiers)
    - Aucune reference $$.Execution.Input dans aucun des fichiers manage_*.asl.json
  </done>
</task>

</tasks>

<verification>
1. `pytest tests/test_asl_validation.py -v --tb=short` — tous les tests passent, aucun echec
2. `grep -rn 'Execution.Input' modules/step-functions/efs/manage_*.asl.json` — 0 resultats
3. Pour chaque fichier manage_*.asl.json : JSON valide, Comment present, Fail state nomme, StartAt valide
4. README.md contient les schemas I/O des 3 sous-SFN et le pattern templatefile
5. `grep 'Placeholder' modules/step-functions/efs/manage_*.asl.json` — 0 resultats (aucun stub restant)
</verification>

<success_criteria>
- Les 3 sous-SFN sont completes et fonctionnelles (aucun stub)
- ManageLambdaLifecycle: ~8 states, gere check/create/wait/verify Lambda
- ManageAccessPoint: ~4 states, gere create/verify access point
- ManageFileSystemPolicy: ~6 states (inchange depuis Plan 02)
- Tous les tests passent (TestASLComment, TestASLCatchSelfContained inclus)
- README.md est complet avec les 3 contrats I/O
- Aucune reference $$.Execution.Input dans les sous-SFN
- La Phase 1 est terminable apres ce plan
</success_criteria>

<output>
After completion, create `.planning/phases/01-extraction/01-03-SUMMARY.md`
</output>
